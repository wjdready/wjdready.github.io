<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Lua 调用 C/C++ 动态库</title>
    <url>/2020/12/20/C%E8%AF%AD%E8%A8%80/lua-c/</url>
    <content><![CDATA[<p>本文主要演示如何编写 C/C++ 动态库并在 Lua 中调用</p>
<span id="more"></span> 
<p>Lua 是一种轻量小巧的脚本语言，用标准C语言编写并以源代码形式开放， 其设计目的是为了嵌入应用程序中，从而为应用程序提供灵活的扩展和定制功能。比如通过调用 C/C++ 编写的动态库可以使 Lua 执行起来非常高效。</p>
<ul>
<li><p>安装 Lua 和 Lua-dev<br>  在下面安装包中 lua5.3 提供了解释器, 而 liblua5.3-dev 提供了 C/C++ 编译环境的头文件和链接库</p>
  <figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo apt-get install lua5.3</span><br><span class="line">sudo apt-get install liblua5.3-dev</span><br></pre></td></tr></table></figure>
</li>
<li><p>hello.c 文件</p>
  <figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;lua.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;lauxlib.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">say_hello</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Hello Lua\n&quot;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 固定写法 luaopen_xxxx (xxxx 为库名称), Lua require 时调用 */</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">luaopen_hello</span><span class="params">(lua_State *L)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/* 注册 Lua 函数 */</span></span><br><span class="line">    luaL_Reg fns [] = &#123;</span><br><span class="line">        &#123;<span class="string">&quot;sayHello&quot;</span>, say_hello&#125;,</span><br><span class="line">        &#123;<span class="literal">NULL</span>, <span class="literal">NULL</span>&#125;</span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 创建 Lua 库 */</span></span><br><span class="line">    luaL_newlib(L, fns);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>Makefile 文件</p>
  <figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">LIBFLAG= -shared -fpic</span><br><span class="line"></span><br><span class="line"><span class="section">hello.so: hello.c</span></span><br><span class="line">    gcc hello.c -o hello.so <span class="variable">$(LIBFLAG)</span> -I/usr/<span class="keyword">include</span>/lua5.3 -llua5.3</span><br></pre></td></tr></table></figure>
</li>
<li><p>test.lua 文件</p>
  <figure class="highlight lua"><table><tr><td class="code"><pre><span class="line"><span class="built_in">package</span>.<span class="built_in">cpath</span> = <span class="string">&quot;./?.so&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">local</span> hello = <span class="built_in">require</span> <span class="string">&quot;hello&quot;</span></span><br><span class="line"></span><br><span class="line">hello.sayHello()</span><br></pre></td></tr></table></figure></li>
<li><p>运行结果</p>
  <figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> make</span></span><br><span class="line">  gcc hello.c -o hello.so -shared -fpic -I/usr/include/lua5.3 -llua5.3</span><br><span class="line"><span class="meta">$</span><span class="bash"> lua test.lua</span></span><br><span class="line">  Hello Lua</span><br></pre></td></tr></table></figure>

</li>
</ul>
<p>说明: 编译参数 -shared -fpic 告诉编译器生成的是动态链接库, 详情参考 <a class="link" href="https://www.jianshu.com/p/68108c046298">C 动态与静态链接库<i class="fas fa-external-link-alt"></i></a></p>
]]></content>
      <categories>
        <category>数据库</category>
      </categories>
      <tags>
        <tag>链接库</tag>
      </tags>
  </entry>
  <entry>
    <title>SDL 播放pcm音频</title>
    <url>/2021/04/10/SDL%E7%BC%96%E7%A8%8B/SDL%E6%92%AD%E6%94%BEPCM%E9%9F%B3%E9%A2%91/</url>
    <content><![CDATA[<p>本文主要介绍如何利用SDL来播放pcm音频</p>
<span id="more"></span>


<h4 id="生成-pcm-音频"><a href="#生成-pcm-音频" class="headerlink" title="生成 pcm 音频"></a>生成 pcm 音频</h4><ul>
<li>mp3 截取<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> ffmpeg -i test.mp3 -ss 00:00:10 -t 00:00:30 -acodec copy cut.mp3</span></span><br></pre></td></tr></table></figure>
<blockquote>
<p>-acodec copy 编码格式复制</p>
</blockquote>
</li>
<li>mp3 转 pcm<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> ffmpeg -i cut.mp3 -acodec pcm_s16le -f s16le -ac 2 -ar 44100 test.pcm</span></span><br></pre></td></tr></table></figure>
<blockquote>
<p>-f 强制文件格式 -ac 通道数 -ar 采用率</p>
</blockquote>
</li>
</ul>
<h4 id="源代码"><a href="#源代码" class="headerlink" title="源代码"></a>源代码</h4><ul>
<li><p>demo.c</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;SDL2/SDL.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">* @brief SDL2 获取音频的回调函数</span></span><br><span class="line"><span class="comment">* @param userdata 用户数据</span></span><br><span class="line"><span class="comment">* @param stream 音频缓冲区</span></span><br><span class="line"><span class="comment">* @param len 音频长度</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">fill_audio_cb</span><span class="params">(<span class="keyword">void</span> *userdata, Uint8 * stream, <span class="keyword">int</span> len)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  FILE **fpp = (FILE**)userdata;</span><br><span class="line">  <span class="keyword">if</span>(fpp == <span class="literal">NULL</span> || *fpp == <span class="literal">NULL</span> || len == <span class="number">0</span>)</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="keyword">return</span> ;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  SDL_memset(stream, <span class="number">0</span>, len);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span>(fread(stream, <span class="number">1</span>, len, *fpp) != len)</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Play Done! Replay\n&quot;</span>);</span><br><span class="line">    fseek(*fpp, <span class="number">0</span>, SEEK_SET);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">  SDL_Init(SDL_INIT_AUDIO | SDL_INIT_TIMER);</span><br><span class="line"></span><br><span class="line">  FILE *audio_file = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">  SDL_AudioSpec aduio_spec = &#123;</span><br><span class="line">    .format = AUDIO_S16LSB,    <span class="comment">/* 音频格式 signed 16bit LSB */</span></span><br><span class="line">    .freq = <span class="number">44100</span>,             <span class="comment">/* 采样率 */</span></span><br><span class="line">    .channels = <span class="number">2</span>,             <span class="comment">/* 通道数 */</span></span><br><span class="line">    .silence = <span class="number">0</span>,              <span class="comment">/* 静音值 */</span></span><br><span class="line">    .callback = fill_audio_cb, <span class="comment">/* 回调函数 */</span></span><br><span class="line">    .userdata = &amp;audio_file    <span class="comment">/* 回调函数用户数据 */</span></span><br><span class="line">  &#125;;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> ((audio_file = fopen(<span class="string">&quot;test.pcm&quot;</span>, <span class="string">&quot;rb&quot;</span>)) == <span class="literal">NULL</span>)</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Cannot open this file\n&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">if</span> (SDL_OpenAudio(&amp;aduio_spec, <span class="literal">NULL</span>) &lt; <span class="number">0</span>)</span><br><span class="line">  &#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Can&#x27;t open audio.\n&quot;</span>);</span><br><span class="line">    fclose(audio_file);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="built_in">printf</span>(<span class="string">&quot;Start to play, return to exit\n&quot;</span>);</span><br><span class="line">  SDL_PauseAudio(<span class="number">0</span>);</span><br><span class="line"></span><br><span class="line">  getchar();</span><br><span class="line">  fclose(audio_file);</span><br><span class="line">  SDL_Quit();</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>编译运行（mingw 环境）</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> gcc demo.c -lmingw32 -lSDL2main -lSDL2</span></span><br><span class="line"><span class="meta">$</span><span class="bash"> ./a.exe</span></span><br><span class="line">Start to play, return to exit</span><br></pre></td></tr></table></figure></li>
</ul>
]]></content>
      <categories>
        <category>SDL 编程</category>
      </categories>
      <tags>
        <tag>pcm</tag>
      </tags>
  </entry>
  <entry>
    <title>MQTT开发</title>
    <url>/2021/04/10/%E5%B5%8C%E5%85%A5%E5%BC%8F/MQTT%E5%BC%80%E5%8F%91/</url>
    <content><![CDATA[<p>本文主要介绍Ubuntu环境下搭建MQTT服务器和简单使用</p>
<span id="more"></span>

<h3 id="Linux-搭建MQTT服务器"><a href="#Linux-搭建MQTT服务器" class="headerlink" title="Linux 搭建MQTT服务器"></a>Linux 搭建MQTT服务器</h3><ul>
<li><p>安装 </p>
  <figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo apt-add-repository ppa:mosquitto-dev/mosquitto-ppa     # 添加源到软件仓库</span><br><span class="line">sudo apt-get update                                         # 更新软件仓库列表</span><br><span class="line">sudo apt-get install mosquitto                              # 安装mosquitto</span><br></pre></td></tr></table></figure></li>
<li><p>试运行</p>
  <figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo service mosquitto status                               # 查看运行状态</span><br><span class="line">sudo service mosquitto start                                # 启动服务</span><br><span class="line">sudo service mosquitto stop                                 # 停止服务</span><br></pre></td></tr></table></figure>
</li>
<li><p>添加和系修改配置</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo service mosquitto stop   #首先停止服务</span><br><span class="line"></span><br><span class="line"><span class="meta">#</span><span class="bash"> 用户的局部配置文件放在： /etc/mosquitto/conf.d/目录下，并且这个目录下的所有以.conf后缀的文件都将被mosquitto作为配置文件，在启动时加载。</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#</span><span class="bash"> 在/etc/mosquitto/conf.d目录下，新建myconfig.conf配置文件，输入以下内容</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#</span><span class="bash"> 关闭匿名访问，客户端必须使用用户名</span></span><br><span class="line">allow_anonymous false</span><br><span class="line"><span class="meta">#</span><span class="bash">指定 用户名-密码 文件</span></span><br><span class="line">password_file /etc/mosquitto/pwfile.txt</span><br></pre></td></tr></table></figure>
</li>
<li><p>创建一个MQTT服务器账户</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> 假设用户名为:user1， 在命令行运行：</span></span><br><span class="line">   </span><br><span class="line">mosquitto_passwd -c /etc/mosquitto/pwfile.txt user1</span><br><span class="line"></span><br><span class="line"><span class="meta">#</span><span class="bash">回车后连续输入2次用户密码即可，然后重新启动 mosquitto</span></span><br><span class="line">sudo service mosquitto start      </span><br></pre></td></tr></table></figure>
</li>
<li><p>测试 mosquitto：可在App Store下载MQTTool工具进行测试。</p>
</li>
<li><p>在线用测试网址参考<a class="link" href="https://blog.csdn.net/cpongo3/article/details/89327641">博客<i class="fas fa-external-link-alt"></i></a></p>
</li>
</ul>
]]></content>
      <categories>
        <category>嵌入式</category>
      </categories>
      <tags>
        <tag>mqtt</tag>
      </tags>
  </entry>
  <entry>
    <title>arm 交叉编译工具</title>
    <url>/2020/12/20/%E5%B5%8C%E5%85%A5%E5%BC%8F/arm%20%E4%BA%A4%E5%8F%89%E7%BC%96%E8%AF%91%E5%B7%A5%E5%85%B7/</url>
    <content><![CDATA[<p>本文主要介绍 arm 交叉编译工具的命名规则和常见的编译器</p>
<span id="more"></span>

<h3 id="命名规则"><a href="#命名规则" class="headerlink" title="命名规则"></a>命名规则</h3><p>交叉编译工具链的命名规则为：<code>arch [-vendor] [-os] [-(gnu)eabi]</code></p>
<ul>
<li>arch - 体系架构，如ARM，MIPS</li>
<li>verdor - 工具链提供商</li>
<li>os - 目标操作系统</li>
<li>eabi - 嵌入式应用二进制接口（Embedded Application Binary Interface (ABI) ）</li>
</ul>
<h3 id="具体编译器"><a href="#具体编译器" class="headerlink" title="具体编译器"></a>具体编译器</h3><ul>
<li><p><strong>arm-none-eabi-gcc</strong></p>
<blockquote>
<p>ARM architecture，no vendor，not target an operating system，complies with the <strong>ARM EABI</strong>（ARM 程序接口）</p>
</blockquote>
<p>用于编译 ARM 架构的裸机系统（包括 ARM Linux 的 boot、kernel）他使用的是<br>newlib 这个专用于嵌入式系统的C库，安装方法：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo apt-get install gcc-arm-none-eabi</span><br></pre></td></tr></table></figure></li>
<li><p><strong>arm-linux-gnueabi-gcc</strong> </p>
<blockquote>
<p>ARM architecture, creates binaries that run on the Linux operating system, and uses the <strong>GNU EABI</strong> （GUN 程序接口）</p>
</blockquote>
<p>也叫 **<code>arm-none-linux-gnueabi-gcc</code>**，因为一开始是没有发行商的，目前由Linaro 公司发行（Linaro 则是由ARM、飞思卡尔、IBM、Samsung、ST-Ericsson和TI等厂商联合，为开发不同半导体公司系统单芯片（SoC）平台的共通软件的一个非盈利公司)，使用glibc，可用于交叉编译ARM系统中所有环节的代码，包括裸机程序、u-boot、Linux kernel、filesystem和App应用程序，安装方法：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo apt-get install gcc-arm-linux-gnueabi</span><br></pre></td></tr></table></figure></li>
<li><p><strong>arm-linux-gnueabihf-gcc</strong></p>
<blockquote>
<p>和 <code>arm-linux-gnueabi-gcc</code> 区别不过是 gcc 的选项 <code>-mfloat-abi</code> 的默认值不同，即浮点数处理上的不同</p>
</blockquote>
<p> <code>-mfloat-abi</code> 有三种值：</p>
<ol>
<li><strong>soft</strong>： 不用fpu进行浮点计算，即使有fpu浮点运算单元也不用，而是使用软件模式</li>
<li><strong>softfp</strong>： armel架构（对应的编译器为 arm-linux-gnueabi-gcc ）采用的默认值，用fpu计算，但是传参数用普通寄存器传，这样中断的时候，只需要保存普通寄存器，中断负荷小，但是参数需要转换成浮点的再计算。</li>
<li><strong>hard</strong>： armhf架构（对应的编译器 arm-linux-gnueabihf-gcc ）采用的默认值，用fpu计算，传参数也用fpu中的浮点寄存器传，省去了转换，性能最好，但是中断负荷高。</li>
</ol>
</li>
<li><p><strong>arm-eabi-gcc</strong><br>Android ARM 编译器</p>
</li>
<li><p><strong>armcc</strong><br>ARM 公司推出的编译工具，功能和 <code>arm-none-eabi-gcc</code> 类似，可以编译裸机程序（u-boot、kernel），但是不能编译 Linux 应用程序</p>
</li>
<li><p><strong>arm-none-uclinuxeabi-gcc</strong><br>arm-none-uclinuxeabi 用于uCLinux，使用Glibc</p>
</li>
<li><p><strong>arm-none-symbianelf-gcc</strong><br>用于symbian。</p>
</li>
</ul>
]]></content>
      <categories>
        <category>嵌入式</category>
      </categories>
      <tags>
        <tag>编译器</tag>
      </tags>
  </entry>
  <entry>
    <title>Ubuntu安装设置</title>
    <url>/2020/12/22/%E5%85%B6%E4%BB%96/Ubuntu%E5%AE%89%E8%A3%85%E8%AE%BE%E7%BD%AE/</url>
    <content><![CDATA[<p>介绍 VMware Ubuntu 安装到使用 </p>
<span id="more"></span>

<h4 id="分区"><a href="#分区" class="headerlink" title="分区"></a>分区</h4><ul>
<li>方案1, 以简单为原则：<table>
<thead>
<tr>
<th align="center">挂载点</th>
<th align="center">分区类型</th>
<th align="center">分区格式</th>
<th align="center">说明</th>
</tr>
</thead>
<tbody><tr>
<td align="center">swap</td>
<td align="center">逻辑分区</td>
<td align="center">swap</td>
<td align="center">充当虚拟内存, 大小等于内存大小</td>
</tr>
<tr>
<td align="center">/</td>
<td align="center">主分区</td>
<td align="center">ext4</td>
<td align="center">安装系统和软件, 大小余下所有</td>
</tr>
</tbody></table>
</li>
<li>方案2, 资深用户：<table>
<thead>
<tr>
<th align="center">挂载点</th>
<th align="center">分区类型</th>
<th align="center">分区格式</th>
<th align="center">说明</th>
</tr>
</thead>
<tbody><tr>
<td align="center">swap</td>
<td align="center">逻辑分区</td>
<td align="center">swap</td>
<td align="center">充当虚拟内存, 大小等于内存大小</td>
</tr>
<tr>
<td align="center">/</td>
<td align="center">主分区</td>
<td align="center">ext4</td>
<td align="center">安装系统和软件, 大小 20GB 以上</td>
</tr>
<tr>
<td align="center">/boot</td>
<td align="center">逻辑分区</td>
<td align="center">ext4</td>
<td align="center">引导分区, 大小 1G 左右</td>
</tr>
<tr>
<td align="center">/home</td>
<td align="center">逻辑分区</td>
<td align="center">ext4</td>
<td align="center">相当于 “我的文档”, 大小为硬盘剩下的</td>
</tr>
</tbody></table>
</li>
</ul>
<h4 id="安装-VMare-Tools"><a href="#安装-VMare-Tools" class="headerlink" title="安装 VMare Tools"></a>安装 VMare Tools</h4><p>  VMare Tools 提供分辨率自适应, 同 win10 共享剪贴板, 直接拖动文件与 win10 交互等高级功能</p>
<ul>
<li>将VMare Tools 镜像安装到系统后复制 VMwareTools 压缩包到系统目录, 执行<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo ./vmware-install.pl</span><br></pre></td></tr></table></figure>
一路默认设置即可</li>
</ul>
<h4 id="添加源和安装必备软件"><a href="#添加源和安装必备软件" class="headerlink" title="添加源和安装必备软件"></a>添加源和安装必备软件</h4><ul>
<li><a class="link" href="https://mirror.tuna.tsinghua.edu.cn/help/ubuntu/">清华源<i class="fas fa-external-link-alt"></i></a> <figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo mv /etc/apt/sources.list /etc/apt/sources.list.bak</span><br><span class="line">sudo vi /etc/apt/sources.list</span><br></pre></td></tr></table></figure>
 复制进去, 保存退出, 更新软件 <figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo apt-get update</span><br></pre></td></tr></table></figure></li>
<li>安装软件<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sudo apt-get install vim git </span><br></pre></td></tr></table></figure>

</li>
</ul>
<h4 id="安装-sougou-输入法"><a href="#安装-sougou-输入法" class="headerlink" title="安装 sougou 输入法"></a>安装 sougou 输入法</h4><ul>
<li>安装 fcitx<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo apt install fcitx</span><br></pre></td></tr></table></figure></li>
<li>下载安装 <a class="link" href="https://pinyin.sogou.com/linux/">sougou 输入法<i class="fas fa-external-link-alt"></i></a> <figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo dpkg -i xxxx.deb   # 安装下载的 deb 包</span><br><span class="line">sudo apt -f install     # 自动安装依赖</span><br></pre></td></tr></table></figure>
 安装完成, 设置–&gt;区域与语言–&gt;管理已安装的语言, 设置为 fcitx, 然后重启即可</li>
</ul>
]]></content>
      <categories>
        <category>其他</category>
      </categories>
      <tags>
        <tag>Ubuntu</tag>
      </tags>
  </entry>
  <entry>
    <title>SDL安装与使用</title>
    <url>/2020/12/20/SDL%E7%BC%96%E7%A8%8B/SDL%E5%AE%89%E8%A3%85%E4%B8%8E%E4%BD%BF%E7%94%A8/SDL%E5%AE%89%E8%A3%85%E4%B8%8E%E4%BD%BF%E7%94%A8/</url>
    <content><![CDATA[<p>本文介绍在 Ubuntu 下 SDL2 库的安装与使用</p>
<span id="more"></span>

<ul>
<li><p>安装C语言开发库</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">sudo apt-get install libsdl2-dev</span><br></pre></td></tr></table></figure>
</li>
<li><p>demo.cpp 文件</p>
  <figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;SDL2/SDL.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdbool.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    SDL_Window *window = SDL_CreateWindow(<span class="string">&quot;demo&quot;</span>, SDL_WINDOWPOS_CENTERED, SDL_WINDOWPOS_CENTERED, <span class="number">640</span>, <span class="number">480</span>, SDL_WINDOW_SHOWN);</span><br><span class="line">    SDL_Renderer *renderer = SDL_CreateRenderer(window, <span class="number">-1</span>, SDL_RENDERER_ACCELERATED);</span><br><span class="line">    <span class="keyword">bool</span> quit = <span class="literal">false</span>;</span><br><span class="line">    SDL_Event event;</span><br><span class="line"></span><br><span class="line">    SDL_SetRenderDrawColor(renderer, <span class="number">255</span>, <span class="number">255</span>, <span class="number">255</span>, <span class="number">255</span>);</span><br><span class="line">    <span class="keyword">while</span> (!quit) &#123;</span><br><span class="line">        <span class="keyword">while</span> (SDL_PollEvent(&amp;event)) &#123;</span><br><span class="line">            <span class="keyword">switch</span> (event.type) &#123;</span><br><span class="line">            <span class="keyword">case</span> SDL_QUIT:</span><br><span class="line">                quit = <span class="literal">true</span>;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">case</span> SDL_MOUSEMOTION:</span><br><span class="line">                <span class="keyword">if</span>(SDL_GetMouseState(<span class="literal">NULL</span>, <span class="literal">NULL</span>) &amp;SDL_BUTTON(<span class="number">1</span>))</span><br><span class="line">                    SDL_RenderDrawPoint(renderer, event.motion.x, event.motion.y);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        SDL_RenderPresent(renderer);</span><br><span class="line">        SDL_Delay(<span class="number">1000</span> / <span class="number">60</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    SDL_DestroyWindow(window);</span><br><span class="line">    SDL_DestroyRenderer(renderer);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>编译运行</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> gcc demo.c -lSDL2</span></span><br><span class="line"><span class="meta">$</span><span class="bash"> ./a.out</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>运行结果<br><img src="/2020/12/20/SDL%E7%BC%96%E7%A8%8B/SDL%E5%AE%89%E8%A3%85%E4%B8%8E%E4%BD%BF%E7%94%A8/SDL%E5%AE%89%E8%A3%85%E4%B8%8E%E4%BD%BF%E7%94%A8/%E8%BF%90%E8%A1%8C%E7%BB%93%E6%9E%9C.png" alt="运行结果"></p>
</li>
</ul>
]]></content>
      <categories>
        <category>SDL 编程</category>
      </categories>
      <tags>
        <tag>SDL</tag>
      </tags>
  </entry>
  <entry>
    <title>MSYS2 的使用</title>
    <url>/2021/04/10/%E5%85%B6%E4%BB%96/MSYS/</url>
    <content><![CDATA[<p>本文主要介绍 MSYS2 的历史和使用</p>
<span id="more"></span>

<h2 id="MSYS2-的前世今生"><a href="#MSYS2-的前世今生" class="headerlink" title="MSYS2 的前世今生"></a>MSYS2 的前世今生</h2><h3 id="Cygwin"><a href="#Cygwin" class="headerlink" title="Cygwin"></a>Cygwin</h3><p>这一切得从源头说起, 1995年Cygnus的一位工程师Steve Chamberlain, 他观察到Windows 系统使用COFF作为目标文件 (即可执行文件) 格式，与此同时GNU的工具链已经支持x86和COFF的目标文件，并提供C语言库newlib (Newlib 是嵌入式系统上的C标准库的实现) 。他认为既然GNU的工具链已经能够编译生成x86指令集的机器码并可链接生成COFF格式的目标文件, 而且还提供了可移植到任意平台的C标准库newlib, 那么理论上只要将GCC重定向(根据对应目标平台重新编译), 作为一个 cross compiler(交叉编译器)，那么这个GCC 编译器不就可以生成 Windows 平台下的可执行文件了吗? 事实证明这是可行的， Steve Chamberlain 很快开发出了原型, 并把他这个项目命名为Cygwin.</p>
<p>现在 GCC 工具链有了，终于可以使用熟悉的GCC而不是微软的那一套MSVC来生成Windows 平台的应用程序了。不过，光有GCC 并不能很好的进行构建工作, 还需要 shell 脚本解释器以及那些与Uinx系统配套的一系列称手的工具也加入进来，因为只有这些工具的加持，GCC 才能用得舒服。因此 Steve Chamberlain 的下一步工作是尝试在 Windows 构建 shell 脚本运行环境, 因此需要一个与 Bourne shell 兼容的命令解释器(比如 bash), 而移植构建shell 解释器需要一个fork系统来实现程序的调用和标准输入/输出。Windows包含类似的功能, 但它们的接口和POSIX不兼容，这么一来Cygwin的库还需要提供与POSIX兼容的应用程序编程接口(API)。为了达到这个目的 Cygwin 提供了一套抽象层 dll，用于将部分 Posix 调用转换成 Windows 的 API 调用，实现相关功能。这里面最典型的，最基本的模拟层就是那个 cygwin1.dll。</p>
<p>这些工作早期都是由Steve Chamberlain一个人来完成, 但是到了1996年，由于看到cygwin可以提供Windows系统上的嵌入式工具链（以往的方案是使用DJGPP），其他工程师也加入了进来。特别吸引人的是，Cygwin可以实现three-way cross-compile(第三方交叉编译)，也就是说可以在远程工作站上使用cygwin来编译其他平台的代码, 而操作工作站的电脑只是一台普通的pc, 该pc 本身不编译代码。由于工作站的CPU普遍更为强大，这比在PC上使用PC的速度更快。1998年起，Cygnus开始将Cygwin包作为产品来提供。</p>
<p>随着 Linux 系统的发展壮大，目前的 Cygwin 已经不仅仅提供 POSIX 兼容，因此也顺带多了更多模拟层的依赖关系。同时，由于它的模拟层实现了相当良好的 Posix 兼容，人们试着将许多重要的 Linux/BSD 应用移植到了Cygwin下，使得Cygwin越来越大，功能也越来越丰富，以至于目前很多人直接把将Linux应用移植到Windows平台的任务都交给了Cygwin（当然，这种移植并非原生）。</p>
<p><strong>总结</strong>：从上面的描述可以看出, Cygwin 诞生之初，Steve Chamberlain 本来可能只是想通过GCC编译出Windows应用来，却因为 Posix 而顺带搞了一坨别的东西过来，最后发展到现在。可以说, Cygwin是运行于Windows平台的POSIX“子系统”，提供Windows下的类Unix环境，并提供将部分 Linux 应用“移植”到Windows平台的开发环境的一套软件。</p>
<h3 id="MinGW"><a href="#MinGW" class="headerlink" title="MinGW"></a>MinGW</h3><p>由于 Cygwin 的编译和调用方式总是需要依赖一层 POSIX 到 Windows API 的中间层, 比起日渐庞大的 Cygwin, 或许一个最小化且不需要中间层 GNU 工具链更能满足一些开发的需求, 于是 Colin Peters 在1998年创建了一个开源项目并撰写了最初的版本, 他将其命名为 mingw32 (Minimalist GNU for W32), 其意思就是Windows 上的最小化GNU 工具链, 这里的Windows被简称为 “W32”, 后来为了避免暗示它仅限于生成32位二进制文件, 后面的 32 被去除，从而变成了MinGW.</p>
<p>在最初的版本，Colin Peters首先为MSVCRT.DLL(Microsoft Visual C Runtime )库提供了一组头文件和一个导入库, 不过这些代码需要依赖和使用Cygwin的某些接口。之后的 Jan-Jaap van der Heijden希望不再依赖于Cygwin库，而是仅仅使用本机的构建环境来构建应用程序，所以Jan-Jaap着手开发了binutils、GCC和make，并生成了第一个无需使用Cygwin即可生成本机代码的编译器、汇编器和链接器工具。Mumit Khan 后来接手了开发工作，向程序包添加了更多Windows特定的功能，包括Anders Norlander的Windows系统标头。2000年，该项目移至SourceForge，以寻求社区的更多帮助并集中开发。 在 SourceForge 上得到了更多开源人士的帮助, MinGW也得到很大的发展，甚至2005年9月MinGW被选为SourceForge的月度最佳项目。不过在与SourceForge对其邮件列表的管理存在分歧之后，2018年，MinGW迁移到OSDN。</p>
<p><strong>总结</strong>: MinGW 的出现是为了能够使用 GCC 工具链来构建不依赖于第三方运行库的应用程序, 相对于Cygwin，它首要特点就是简单性和性能好，因此，它不提供某些不能使用Windows API轻松实现的POSIX API，如fork()、mmap()和ioctl()。而用Cygwin编写的Windows程序总是需要运行在兼容层DLL之上，因此该DLL必须与程序的源代码一起分发。MinGW不需要兼容层，因为基于MinGW的程序是通过直接调用Windows API编译的。</p>
<h3 id="MSYS"><a href="#MSYS" class="headerlink" title="MSYS"></a>MSYS</h3><p>MinGW 出现之后, 为了更方便地使用 GNU 工具链, 需要提供一个可执行 shell 脚本的运行环境从而可以在shell 中调用MinGW 提供的那些工具如 gcc, make, linkr, 于是诞生了 MSYS(Minimal SYStem)。不过和 Cygwin 追求大而全的 POSIX 兼容系统不同, 其中间层 msys.dll 只提供基本的 POSIX API。因此MSYS可以看做是 Cygwin 的简化版, 但是因为一般 MSYS 都集成了MinGW, 所以当使用MSYS来构建应用程序时如果不使用一些特殊的 POSIX API时, 比如 fork, mmap 其实就是在使用 MinGW 编译代码, 其程序是通过直接调用Windows API编译的, 所以可以不需要依赖任何中间层和第三方DLL库, 也就是说, 这时 MSYS 在这个编译过程只是作为一个辅助工具箱来使用。</p>
<p><strong>总结</strong>: MSYS 环境虽然也像 Cygwin 一样提供 POSIX API, 但是因为本着注重 Minimal(最小化)的原则, 其只提供了最基本的 API, 其兼容性相对于 Cygwin 较差, 但相对于庞大的 Cygwin, 结合 MinGW 的MSYS 可以编译不依赖于中间层DLL的应用程序，执行效率也更高，这个过程MSYS就相当于一个工具箱而已了。</p>
<h3 id="MinGW-w64"><a href="#MinGW-w64" class="headerlink" title="MinGW-w64"></a>MinGW-w64</h3><p>可能是由于维护MinGW的人员工作太过于繁忙, 或是其他原因，MinGW 在后来更新速度异常缓慢，而且当初从ming32 改名为 MinGW, 去掉 32 是为了避免它暗示仅限于生成32位二进制文件, 这就意味着在创建项目的时候开发人员还考虑到将来会更新64位版本, 但是直到现在, MinGW 仍然只支持生成32位的Windows程序, 甚至开发人员以及不打算让其支持 64位的程序了。</p>
<p>2005年，因为最初的MinGW项目没有及时更新其代码库，包括包括几个关键的新API和急需的64位支持，OneVision Software 就创建了一个 MinGW 分支, 并把它命名为 MinGW-w64, 这里的 w64 是 windows 64 位的意思, 代表着支持生成 Windows 64位程序的MinGW, 但不限于64位, MinGW-w64 可以生成 32 位或 64 位的Windows 应用程序。</p>
<p>完成 64位的支持和API更新后 OneVision 将 MinGW-w64 代码提交给MinGW原始项目维护人员，但他们由于怀疑OneVision使用非公开或专有信息而将其拒绝。之后的 2008年，OneVision 将代码捐赠给了其主要开发人员之一的Kai Tietz, 其条件是该代码仍保持开源状态。由于许多原因，后来的MinGW-W64项目的主要开发人员和联合创始人Kai Tietz决定不再尝试与MinGW进一步合作了。</p>
<p>相对于 MinGW, MinGW-w64提供了更完整的Win32 API实现, 但是，MinGW-w64项目不提供官方的二进制构建，不过可以从开发人员的个人构建目录或者从相关但独立的项目（如tdm-gcc或mingw-builds或msys2）中获取。</p>
<p><strong>总结</strong>: 因为 MinGW 更新缓慢以及不支持生成Windows 64位系统程序, 这才诞生了 MinGW-w64, 因此可以说MinGW-w64 就是 MinGW 的升级版, 是基于MinGW 的一个分支, 所以功能和MinGW一样，生成的程序是通过直接调用Windows API编译的而不需要中间层。</p>
<h3 id="MSYS2"><a href="#MSYS2" class="headerlink" title="MSYS2"></a>MSYS2</h3><p>MSYS2(Minimal SYStem 2)是对MSYS的独立重写，和MinGW-w64 的诞生情况基本一致，因为 MSYS 太老了, 更新缓慢, 这才有了MSYS2，不过, 它是基于现代Cygwin和MinGW-w64，它的作用与以前的MSYS在MinGW中所起的作用相同。</p>
<p>作为项目，Cygwin和MSYS2有着明显不同的目标。</p>
<p>Cygwin试图为Windows带来一个与POSIX兼容的环境，这样在Unices上运行的大多数软件都可以在Cygwin上构建和运行，而无需进行任何重大修改。Cygwin提供了大量包含此类软件的软件包，以及用于开发这些软件的库。</p>
<p>MSYS2试图为构建本机Windows软件提供环境。MSYS2提供了包含此类软件的大量软件包，以及用于开发这些软件的库。由于该软件的很大一部分使用GNU构建工具，而GNU构建工具与Unix紧密耦合，因此该环境也与POSIX兼容，并且实际上是基于Cygwin的。</p>
<p>MSYS2提供了运行AutoTools和其他构建系统所需的最小外壳，这些构建系统从互联网上从不同的存储库获取软件源代码，并对其进行配置和构建。外壳和核心工具的存在主要是为了允许移植Unix程序在Windows上本地运行(即不需要POSIX模拟层)。MSYS2不会在必要时重复Cygwin的工作，因此提供的POSIX仿真软件的数量非常少。</p>
<p>MSYS2使用Pacman(出自Arch Linux)来管理其软件包，并附带三个不同的软件包存储库：<br>Msys2：包含依赖于MSYS2的软件。<br>Mingw64：包含64位原生Windows软件(使用mingw-W64 x86_64工具链编译)。<br>Mingw32：包含32位原生Windows软件(使用mingw-W64 i686工具链编译)。</p>
<p>总结: MSYS2 可看做是 MSYS 的升级版，它的作用与以前的MSYS在MinGW中所起的作用相同，并且基于MinGW-w64, 因此可以编译运行 32位和 64 位的程序。同时 MSYS2 使用 pacman 来管理软件包, 并且包含三个不同的软件包存储库，这意味着我们可以在不同的软件存储库之间切换。</p>
<h2 id="MSYS2-使用"><a href="#MSYS2-使用" class="headerlink" title="MSYS2 使用"></a>MSYS2 使用</h2><h3 id="三个运行环境"><a href="#三个运行环境" class="headerlink" title="三个运行环境"></a>三个运行环境</h3><p>安装自然不用说, 安装完成后生成三个快捷图标，</p>
<ul>
<li>MSYS MinGW 32-bit</li>
<li>MSYS MinGW 64-bit</li>
<li>MSYS2 MSYS</li>
</ul>
<p>对快捷图标进行查看属性, 我们可以看到其打开时执行的命令：</p>
<ul>
<li>MSYS MinGW 32-bit 快捷图标的目标是:<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">C:\Library\win64\msys64\msys2_shell.cmd -mingw32</span><br></pre></td></tr></table></figure></li>
<li>MSYS MinGW 64-bit 快捷图标的目标是:<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">C:\Library\win64\msys64\msys2_shell.cmd -mingw64</span><br></pre></td></tr></table></figure></li>
<li>MSYS2 MSYS 快捷图标的目标是:<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">C:\Library\win64\msys64\msys2_shell.cmd -msys</span><br></pre></td></tr></table></figure>

</li>
</ul>
<p>因此可以知道, 切换三个不同的环境是通过向脚本文件msys2_shell.cmd 中传递不同的参数实现的。于是我们在命令行中输入上面的任何一句话:</p>
<figure class="highlight cmd"><table><tr><td class="code"><pre><span class="line"><span class="function">C:\<span class="title">Users</span>\<span class="title">shino</span>&gt; <span class="title">C</span>:\<span class="title">Library</span>\<span class="title">win64</span>\<span class="title">msys64</span>\<span class="title">msys2_shell.cmd</span> -<span class="title">mingw64</span></span></span><br><span class="line"><span class="function"><span class="title">C</span>:\<span class="title">Users</span>\<span class="title">shino</span>&gt;</span></span><br></pre></td></tr></table></figure>
<blockquote>
<p>立刻弹出一个页面，进入 MSYS2 提供的默认终端, 环境显示的是 MINGW64 的</p>
</blockquote>
<p>由此确定MSYS2 提供了一个msys2_shell.cmd 然后该批处理脚本可接受参数, 通过输入的参数来用于切换三个不同的环境类型。</p>
<p>那么如果不输入参数直接运行会怎么样:</p>
<figure class="highlight cmd"><table><tr><td class="code"><pre><span class="line"><span class="function">C:\<span class="title">Users</span>\<span class="title">shino</span>&gt; <span class="title">C</span>:\<span class="title">Library</span>\<span class="title">win64</span>\<span class="title">msys64</span>\<span class="title">msys2_shell.cmd</span></span></span><br><span class="line"><span class="function"><span class="title">C</span>:\<span class="title">Users</span>\<span class="title">shino</span>&gt;</span></span><br></pre></td></tr></table></figure>
<blockquote>
<p>也弹出了窗口, 环境显示的是 MSYS</p>
</blockquote>
<p>也就是说, 默认的环境是 MSYS 环境</p>
<h3 id="msys2-shell-cmd-探索"><a href="#msys2-shell-cmd-探索" class="headerlink" title="msys2_shell.cmd 探索"></a>msys2_shell.cmd 探索</h3><p>既然 msys2_shell.cmd 可接受参数, 那么还可以接受那些参数呢? 一般来说命令程序都会提供一个 -h 或者 -help(–help)参数来提供给使用者提供帮助的, 于是尝试在cmd中输入如下内容:</p>
<figure class="highlight cmd"><table><tr><td class="code"><pre><span class="line"><span class="function">C:\<span class="title">Users</span>\<span class="title">shino</span>&gt; <span class="title">C</span>:\<span class="title">Library</span>\<span class="title">win64</span>\<span class="title">msys64</span>\<span class="title">msys2_shell.cmd</span> -<span class="title">help</span></span></span><br><span class="line"><span class="function"><span class="title">C</span>:\<span class="title">Users</span>\<span class="title">shino</span>&gt;</span></span><br></pre></td></tr></table></figure>
<blockquote>
<p>出现如下内容:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Usage:</span><br><span class="line">   msys2_shell.cmd [options] [login shell parameters]</span><br><span class="line"></span><br><span class="line">Options:</span><br><span class="line">   -mingw32 | -mingw64 | -msys[2]   Set shell type</span><br><span class="line">   -defterm | -mintty | -conemu     Set terminal type</span><br><span class="line">   -here                            Use current &gt;directory as working</span><br><span class="line">                                    directory</span><br><span class="line">   -where DIRECTORY                 Use specified &gt;DIRECTORY as working</span><br><span class="line">                                    directory</span><br><span class="line">   -[use-]full-path                 Use full current PATH variable</span><br><span class="line">                                    instead of trimming to minimal</span><br><span class="line">   -no-start                        Do not use &quot;start&quot; command and</span><br><span class="line">                                    return login shell resulting</span><br><span class="line">                                    errorcode as this batch file</span><br><span class="line">                                    resulting errorcode</span><br><span class="line">   -shell SHELL                     Set login shell</span><br><span class="line">   -help | --help | -? | &#x2F;?         Display this help and exit</span><br><span class="line"></span><br><span class="line">Any parameter that cannot be treated as valid option and all</span><br><span class="line">following parameters are passed as login shell command parameters.</span><br></pre></td></tr></table></figure>
</blockquote>
<p>可以清楚地看到所有可接受的参数和其使用说明</p>
<ul>
<li><p>第一条是自然不必说, 就是设置我们的shell环境类型的.</p>
</li>
<li><p>第二条是设置终端类型, 看这<code>-defterm</code>应该是default terminal(默认终端)的意思吧, 尝试一下:</p>
<figure class="highlight cmd"><table><tr><td class="code"><pre><span class="line"><span class="function">C:\<span class="title">Users</span>\<span class="title">shino</span>&gt; <span class="title">C</span>:\<span class="title">Library</span>\<span class="title">win64</span>\<span class="title">msys64</span>\<span class="title">msys2_shell.cmd</span> -<span class="title">defterm</span></span></span><br><span class="line"><span class="function"><span class="title">C</span>:\<span class="title">Users</span>\<span class="title">shino</span>&gt;</span></span><br></pre></td></tr></table></figure>
<blockquote>
<p>仍旧弹出一个页面, 然而终端类型变成微软默认的终端了, 因为看左上角的图标, 已经不是 MSYS2 提供的终端了. </p>
</blockquote>
<p>用同样的方法, 在cmd 中分别调用C:\Library\win64\msys64\msys2_shell.cmd批处理并输入<code>-mintty</code>和<code>-conemu</code> 参数观察到:</p>
<blockquote>
<p>输入 <code>-mintty</code>参数时, 弹出的窗口和不输入<code>-mintty</code>时是一致的, 由此得知 MSYS2 默认确实是使用 mintty 作为默认的终端, 输入<code>-conemu</code> 参数时, cmd 出现</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ConEmu not found. Exiting.</span><br></pre></td></tr></table></figure>
<p>这说明 MSYS2 并没有集成ConEmu终端，可能需要用户自己主动去下载才能使用.</p>
</blockquote>
</li>
<li><p>第三条和第四条参数显而易见, 当我们在终端打开 MSYS2 的某一个环境时, 通过 <code>-here</code> 参数可以使MSYS2 环境打开后立刻进入当前命令执行的环境, 如果没有 <code>-here</code>，参数, 则默认会进入 HOME 目录，而输入 <code>-where </code> + DIRECTORY 则可以进入指定文件夹. </p>
</li>
<li><p><code>-use-full-path</code>(-full-path) 参数根据描述应该是将整个Windows 的PATH 添加到MSYS2 环境中的可选参数, 也就是说, 进入MSYS2 环境时，默认是把Windows 下的PATH环境给部分屏蔽掉的, 使用full-path将把PATH的所有内容添加进来, 这里可以验证一下, 我电脑安装了VSCode 并把其添加到了环境变量中去了, 这样一来只要在终端输入 code+[dir] 就可以打开VSCode 编辑器了。那么首先在不使用 <code>-full-path </code>参数进入 MSYS2 环境：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">C:\Users\shino&gt; C:\Library\win64\msys64\msys2_shell.cmd</span><br><span class="line">C:\Users\shino&gt;</span><br></pre></td></tr></table></figure>
<p>进入 MSYS2 环境后输入 code</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">shino@DESKTOP-2E1H5DE MSYS ~</span><br><span class="line">$ code</span><br><span class="line">bash: $&#39;\302\203code&#39;: 未找到命令</span><br><span class="line">shino@DESKTOP-2E1H5DE MSYS ~</span><br><span class="line">$</span><br></pre></td></tr></table></figure>
<p>可以看到 MSYS2 并没能找到 code, 那么使用 <code>-full-path</code> 参数进入MSYS环境:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">C:\Users\shino&gt; C:\Library\win64\msys64\msys2_shell.cmd -full-path</span><br><span class="line">C:\Users\shino&gt;</span><br></pre></td></tr></table></figure>
<p>进入 MSYS2 环境后输入 code</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">shino@DESKTOP-2E1H5DE MSYS ~</span><br><span class="line">$ code</span><br><span class="line">shino@DESKTOP-2E1H5DE MSYS ~</span><br><span class="line">$</span><br></pre></td></tr></table></figure>
<blockquote>
<p>VSCode 编辑器已经打开, 并且程序正常执行并返回了。</p>
</blockquote>
<p>说明想要让MSYS2拥有完整的Windows PATH只需在使用<code>msys2_shell.cmd</code>进入环境时加入参数 <code>-full-path</code>即可，这也是官方默认且推荐的做法。除此之外, 还可以想到的方法是在 .bashrc 通过 export 手动来添加, 这种添加就比较灵活并且有更多的选择性。</p>
<p>其实MSYS2 默认是有继承一部分 Windows 的环境变量的, 这个在我尝试使用 <code>explorer</code> 命令来打开某个文件夹时注意到了, 因为我在没有加入 <code>-full-path</code> 的情况下执行Windows 环境下的命令 <code>explorer</code> . 也是执行成功的。然后我通过 <code>where</code> 命令查看了 <code>explorer</code> 的位置:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">shino@DESKTOP-2E1H5DE MSYS ~</span><br><span class="line">$ where explorer</span><br><span class="line">C:\Windows\explorer.exe</span><br><span class="line">shino@DESKTOP-2E1H5DE MSYS ~</span><br><span class="line">$</span><br></pre></td></tr></table></figure></li>
<li><p><code>-no-start</code> 根据描述, 就是不需要再启动终端程序而是直接在当前终端进入 MSYS2 环境, 这一点非常不错, 因为很多时候我们只是想在当前终端下进入MSYS2 环境然后执行一些任务, 频繁地弹出弹框会让人厌烦。并且该参数使得 MSYS2 可以更好地和VSCode 编辑器或者其他编译器进行更好地集成和交互。打开cmd 测试一下:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">C:\Users\shino&gt; C:\Library\win64\msys64\msys2_shell.cmd -defterm -no-start</span><br><span class="line">shino@DESKTOP-2E1H5DE MSYS ~</span><br><span class="line">$</span><br></pre></td></tr></table></figure>
<blockquote>
<p>不再弹出弹框, 而是直接原地进入 MSYS2 环境</p>
</blockquote>
<p>需要注意的是: 在 cmd 里进入MSYS2 环境需要把终端类型设置成 <code>-defterm</code> 因为MSYS2 默认使用的是 <code>mintty</code> 终端, 因此没有 <code>-defterm</code> 它会先打开 <code>mintty</code>终端，然后执行 <code>no-start</code> 参数, 这不是我们想要的结果。</p>
</li>
<li><p>最后一个参数 -shell 指定了进入环境时使用的 shell 解释器的类型, 默认是 bash。例如，当我们想使用当前比较流行的 <code>zsh</code>, 可以在启动时传入如下参数:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">C:\Users\shino&gt; C:\Library\win64\msys64\msys2_shell.cmd -shell zsh</span><br><span class="line">C:\Users\shino&gt;</span><br></pre></td></tr></table></figure>
<p>当然, 前提是你已经安装了zsh解释器, 在后面, 我们将讨论在 MSYS2 通过使用 pacman 包管理工具来进行方便的安装和卸载。</p>
</li>
</ul>
<h3 id="msys2-shell-cmd-使用"><a href="#msys2-shell-cmd-使用" class="headerlink" title="msys2_shell.cmd 使用"></a>msys2_shell.cmd 使用</h3><p>通过上面对 msys2_shell.cmd 的了解, 我们就可以更方便地使用 MSYS2 了, 下面举例几个比较典型的用法:</p>
<ol>
<li>作为 VSCode 终端的环境: 我们想要实现在 VSCode 中打开终端时就默认进入了 MSYS2 的环境, 那么我们可以在<code>setting.json</code> 文件中添加如下内容: <figure class="highlight"><table><tr><td class="code"><pre><span class="line">&quot;terminal.integrated.shell.windows&quot;: &quot;C:\\Library\\win64\\msys64\\msys2_shell.cmd&quot;,    </span><br><span class="line">&quot;terminal.integrated.shellArgs.windows&quot;: [&quot;-defterm&quot;, &quot;-mingw64&quot;, &quot;-no-start&quot;, &quot;-here&quot;],</span><br></pre></td></tr></table></figure></li>
<li>按需进入 MSYS2 环境: 我们想要实现打开默认cmd的时候并不会立刻进入 MSYS2 环境, 而是在任何时刻我们想进入MSYS2 环境时只需输入 msys 就可以原地进入 MSYS2 环境, 这也是我比较推荐的做法。那么只需要新建一个 <code>msys.bat</code> 文件, 然后将其所在的目录放入环境变量 PATH 中即可: <figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">/c/Library/common $ ls</span><br><span class="line">msys.bat</span><br><span class="line">/c/Library/common $ cat msys.bat</span><br><span class="line">@C:\Library\win64\msys64\msys2_shell.cmd -defterm -mingw64 -no-start -here -shell zsh</span><br><span class="line">/c/Library/common $ </span><br></pre></td></tr></table></figure>
我将 <code>msys.bat</code> 放到了 <code>/c/Library/common</code> 文件夹中, 内容如 cat 显示的那样, 只包含了一行命令就可以完成我们想要的工作, 将  <code>/c/Library/common</code> 文件夹加入到Windows 的PATH 环境变量中就可以在cmd中通过调用 msys 来切换MSYS2 的环境:<figure class="highlight cmd"><table><tr><td class="code"><pre><span class="line"><span class="function">C:\<span class="title">Users</span>\<span class="title">shino</span>&gt; <span class="title">msys</span></span></span><br><span class="line"><span class="function">~ » </span></span><br></pre></td></tr></table></figure>
<blockquote>
<p>在 cmd 中输入msys 后终端提示符变成了<code>~ » </code>，这是因为我将默认 shell 设置成了 zsh 的结果</p>
</blockquote>
</li>
</ol>
<h3 id="设置-HOME-目录"><a href="#设置-HOME-目录" class="headerlink" title="设置 HOME 目录"></a>设置 HOME 目录</h3><p>有时需要指定MSYS2 的home 目录的位置, 但是 <code>msys2_shell.cmd</code> 似乎没有这个选项, 目前只能想到的方案是修改 <code>msys2_shell.cmd</code>文件, 在文件较前面添加这样一行:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">set &quot;HOME&#x3D;%USERPROFILE%&quot;</span><br></pre></td></tr></table></figure>
<blockquote>
<p>= 后面是可选的目录, 这里将其设置得和 Windows 默认的一样</p>
</blockquote>
<h3 id="pacman-包管理"><a href="#pacman-包管理" class="headerlink" title="pacman 包管理"></a>pacman 包管理</h3><p>MSYS2 提供了非常方便的 pacman 包管理工具, 这个工具来自于 ArchLinux, 因此各种命令参数都是通用的。不过由于 MSYS2 提供了三个运行环境, 因此 pacman 管理的软件包也分三个来源，这三个源分别由一下三个文件管理:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;etc&#x2F;pacman.d&#x2F;mirrorlist.mingw32</span><br><span class="line">&#x2F;etc&#x2F;pacman.d&#x2F;mirrorlist.mingw64</span><br><span class="line">&#x2F;etc&#x2F;pacman.d&#x2F;mirrorlist.msys</span><br></pre></td></tr></table></figure>
<p>国内使用 pacman 一般需要将软件源设置成国内的源，这样下载更新才会比较快，例如使用<a class="link" href="https://mirror.tuna.tsinghua.edu.cn/help/msys2/">清华源<i class="fas fa-external-link-alt"></i></a>:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">编辑 &#x2F;etc&#x2F;pacman.d&#x2F;mirrorlist.mingw32 ，在文件开头添加：</span><br><span class="line">Server &#x3D; https:&#x2F;&#x2F;mirrors.tuna.tsinghua.edu.cn&#x2F;msys2&#x2F;mingw&#x2F;i686</span><br><span class="line"></span><br><span class="line">编辑 &#x2F;etc&#x2F;pacman.d&#x2F;mirrorlist.mingw64 ，在文件开头添加：</span><br><span class="line">Server &#x3D; https:&#x2F;&#x2F;mirrors.tuna.tsinghua.edu.cn&#x2F;msys2&#x2F;mingw&#x2F;x86_64</span><br><span class="line"></span><br><span class="line">编辑 &#x2F;etc&#x2F;pacman.d&#x2F;mirrorlist.msys ，在文件开头添加：</span><br><span class="line">Server &#x3D; https:&#x2F;&#x2F;mirrors.tuna.tsinghua.edu.cn&#x2F;msys2&#x2F;msys&#x2F;$arch</span><br><span class="line"></span><br><span class="line">然后执行 pacman -Sy 刷新软件包数据即可。</span><br></pre></td></tr></table></figure>

<p>因为 pacman 同时管理三个环境的软件源，因此在安装卸载等操作时需要提供完整的软件包名，如果不是完整的软件包名称，则pacman 默认操作的是 msys 环境下的软件, 例如 gcc， 三个环境的名称分别是:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">MSYS: gcc</span><br><span class="line">mingw64: mingw-w64-x86_64-gcc</span><br><span class="line">mingw32: mingw-w64-i686-gcc</span><br></pre></td></tr></table></figure>

<p>可以看到, 如果想要让 pacman 安装 mingw64环境下的gcc 则需要在gcc 前加入前置 <code>mingw-w64-x86_64- </code>, 其意义也十分的明显, mingw-w64就是我们上面所说的由OneVision 创建的MinGW-w64 环境, 它支持64位和32位, 因此x86_64则代表64位环境程序, i686则代表32 位的。</p>
<p>使用方法参考<a class="link" href="https://www.jianshu.com/p/47cce454f200">pacman<i class="fas fa-external-link-alt"></i></a></p>
<p>值得注意的是, 在 mingw64或mingw32 环境中, 如果对应环境的程序没有安装,则默认会去调用 MSYS 环境的程序, 例如 gcc 只安装了 MSYS 版本的, 在mingw64 环境中并没有安装gcc，但是输入 gcc 也可以执行, 因为这时候执行的是 MSYS 环境下的 gcc 程序，如果安装了自己环境下的gcc ，则调用的gcc 就变成当前环境下的gcc了。</p>
<h3 id="举例-SDL2-安装和使用"><a href="#举例-SDL2-安装和使用" class="headerlink" title="举例: SDL2 安装和使用"></a>举例: SDL2 安装和使用</h3><p>使用 pacman 安装64位 SDL2 库并编译运行</p>
<ul>
<li><p>安装 SDL2 </p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">pacman -S mingw-w64-x86_64-SDL2</span><br></pre></td></tr></table></figure></li>
<li><p>demo.c</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;SDL2/SDL.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdbool.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    SDL_Window *window = SDL_CreateWindow(<span class="string">&quot;demo&quot;</span>, SDL_WINDOWPOS_CENTERED, SDL_WINDOWPOS_CENTERED, <span class="number">640</span>, <span class="number">480</span>, SDL_WINDOW_SHOWN);</span><br><span class="line">    SDL_Renderer *renderer = SDL_CreateRenderer(window, <span class="number">-1</span>, SDL_RENDERER_ACCELERATED);</span><br><span class="line">    <span class="keyword">bool</span> quit = <span class="literal">false</span>;</span><br><span class="line">    SDL_Event event;</span><br><span class="line">    </span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Hello World\n&quot;</span>);</span><br><span class="line">    </span><br><span class="line">    SDL_SetRenderDrawColor(renderer, <span class="number">255</span>, <span class="number">255</span>, <span class="number">255</span>, <span class="number">255</span>);</span><br><span class="line">    <span class="keyword">while</span> (!quit) &#123;</span><br><span class="line">        <span class="keyword">while</span> (SDL_PollEvent(&amp;event)) &#123;</span><br><span class="line">            <span class="keyword">switch</span> (event.type) &#123;</span><br><span class="line">            <span class="keyword">case</span> SDL_QUIT:</span><br><span class="line">                quit = <span class="literal">true</span>;</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            <span class="keyword">case</span> SDL_MOUSEMOTION:</span><br><span class="line">                <span class="keyword">if</span>(SDL_GetMouseState(<span class="literal">NULL</span>, <span class="literal">NULL</span>) &amp;SDL_BUTTON(<span class="number">1</span>))</span><br><span class="line">                    SDL_RenderDrawPoint(renderer, event.motion.x, event.motion.y);</span><br><span class="line">                <span class="keyword">break</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        SDL_RenderPresent(renderer);</span><br><span class="line">        SDL_Delay(<span class="number">1000</span> / <span class="number">60</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    SDL_DestroyWindow(window);</span><br><span class="line">    SDL_DestroyRenderer(renderer);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>编译运行</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">~&#x2F;Desktop&#x2F;SDL » gcc demo.c -lmingw32 -lSDL2main -lSDL2 -mwindows</span><br><span class="line">~&#x2F;Desktop&#x2F;SDL » .&#x2F;a.exe </span><br><span class="line">~&#x2F;Desktop&#x2F;SDL » </span><br></pre></td></tr></table></figure>
</li>
</ul>
]]></content>
      <categories>
        <category>其他</category>
      </categories>
      <tags>
        <tag>MSYS2</tag>
      </tags>
  </entry>
  <entry>
    <title>Sqlite3 C 语言</title>
    <url>/2020/12/20/%E6%95%B0%E6%8D%AE%E5%BA%93/Sqlite3/</url>
    <content><![CDATA[<p>本文是 SQLite 数据库的 C 编程教程，它介绍了使用 C 语言进行 SQLite 编程的基础知识<span id="more"></span><br>更多信息访问 <a class="link" href="http://zetcode.com/db/sqlite/">SQLite tutorial<i class="fas fa-external-link-alt"></i></a> 教程</p>
<h2 id="SQL数据库"><a href="#SQL数据库" class="headerlink" title="SQL数据库"></a>SQL数据库</h2><ul>
<li>SQLite 是一个支持嵌入式的<strong>关系型数据库引擎</strong>。它的开发人员称其为自包含(self-contained)、无服务器(serverless)、零配置和事务性的 SQL 数据库引擎。</li>
<li>它目前非常流行，今天全世界有数亿设备在使用。SQLite 用于 Solaris 10、Mac OS、Android 或 iPhone。</li>
<li>Qt4 库具有对 SQLite 以及 Python 和 PHP 的内置支持。</li>
<li>许多流行的应用程序在内部使用 SQLite，比如 Firefox、Google Chrome 或 Amarok。</li>
</ul>
<h2 id="SQLite3-工具"><a href="#SQLite3-工具" class="headerlink" title="SQLite3 工具"></a>SQLite3 工具</h2><p>SQLite3 工具是 SQLite 库的基于终端的前端。它以交互方式来分析用户提供的查询 (queries) 操作，并以多种格式显示结果。它还可以在脚本中使用。它有自己的一组元命令，包括 <code>.tables</code>、<code>.load</code>、<code>.database</code> 或<code>.dump</code>。要获取所有指令的列表，我们只需键入. help 命令。</p>
<p>现在，我们将使用 sqlite3 工具创建一个新数据库。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> sqlite3 test.db</span></span><br><span class="line">SQLite version 3.22.0 2018-01-22 18:45:57</span><br><span class="line">Enter &quot;.help&quot; for usage hints.</span><br></pre></td></tr></table></figure>

<p>这里，我们为 sqlite3 工具提供了一个参数；test.db 是一个数据库名称。这是我们磁盘上的一个文件。如果它存在，则会将其打开。如果不是，则创建它。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">sqlite&gt;</span><span class="bash"> .tables</span></span><br><span class="line"><span class="meta">sqlite&gt;</span><span class="bash"> .<span class="built_in">exit</span></span></span><br></pre></td></tr></table></figure>

<p><code>tables</code> 命令提供 test.db 数据库中所有存在的的**表 (table)**。当前没有表，因为我们没有创建它。<code>exit</code> 命令终止 sqlite3 命令行工具的交互会话。</p>
<p>退出后，通过 unix 的 <code>ls</code> 命令显示当前工作目录的内容。我们可以看到 test.db 文件。数据库会把所有数据都将存储在此单个文件中。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> ls</span></span><br><span class="line">test.db</span><br></pre></td></tr></table></figure>

<h2 id="C99"><a href="#C99" class="headerlink" title="C99"></a>C99</h2><p>本教程使用 C99。对于 GNU C 编译器，我们需要使用 <code>-std=c99</code> 选项。对于 Windows 用户，推荐使用 PELES C IDE。(MSVC 不支持 C99。)。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> rc=sqlite3_open(“test.db”，&amp;db)；</span><br></pre></td></tr></table></figure>
<p>在 C99 中，我们可以将声明与代码混合使用。但在旧的 C 程序中，我们需要将此行分成两行。</p>
<h2 id="获取-SQLite-版本"><a href="#获取-SQLite-版本" class="headerlink" title="获取 SQLite 版本"></a>获取 SQLite 版本</h2><p>在第一个代码示例中，我们将获得 SQLite 数据库的版本。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// -- 获取 SQLite 版本 v1 --</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sqlite3.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%s\n&quot;</span>, sqlite3_libversion()); </span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>sqlite3_libversion</code> 函数的作用是：返回一个指示 SQLite 库的字符串。</p>
<p>头文件 <code>sqlite3.h</code> 定义了 SQLite 库提供给客户端程序的接口。它包含定义、函数原型和注释。它是 SQLite API 的权威来源。</p>
<p>我们使用 GNU C 编译器编译程序。然后运行它</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> gcc -o version version.c -lsqlite3 -std=c99</span></span><br><span class="line"><span class="meta">$</span><span class="bash"> ./version</span></span><br><span class="line">3.22.0</span><br></pre></td></tr></table></figure>

<p>在第二个示例中，我们再次获取 SQLite 数据库的版本。但这次我们将使用 SQL 的查询 (query) 功能。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// -- 获取 SQLite 版本 v2 --</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sqlite3.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    sqlite3 *db;</span><br><span class="line">    sqlite3_stmt *pStmt;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 打开数据库</span></span><br><span class="line">    <span class="keyword">if</span> (sqlite3_open(<span class="string">&quot;:memory:&quot;</span>, &amp;db) != SQLITE_OK) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;Cannot open database: %s\n&quot;</span>, sqlite3_errmsg(db));</span><br><span class="line">        sqlite3_close(db);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 编译 SQL 语句</span></span><br><span class="line">    <span class="keyword">if</span> (sqlite3_prepare_v2(db, <span class="string">&quot;SELECT SQLITE_VERSION()&quot;</span>, <span class="number">-1</span>, &amp;pStmt, <span class="number">0</span>) != SQLITE_OK) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;Failed to fetch data: %s\n&quot;</span>, sqlite3_errmsg(db));</span><br><span class="line">        sqlite3_close(db);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 执行 SQL 语句</span></span><br><span class="line">    <span class="keyword">if</span> (sqlite3_step(pStmt) == SQLITE_ROW) </span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%s\n&quot;</span>, sqlite3_column_text(pStmt, <span class="number">0</span>));</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 释放 SQL 语句句柄并关闭数据库</span></span><br><span class="line">    sqlite3_finalize(pStmt);</span><br><span class="line">    sqlite3_close(db);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p><code>SQLITE_VERSION()</code> 查询用于获取 SQLite 库的版本。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">sqlite3 *db;</span><br><span class="line">sqlite3_stmt *pStmt;</span><br></pre></td></tr></table></figure>

<p><code>sqlite3</code> 结构定义了一个数据库句柄。每个打开的 SQLite 数据库由一个数据库句柄表示。<br><code>sqlite3_stmt</code> 结构表示单个 SQL 语句 (statement)</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">sqlite3_open(<span class="string">&quot;:memory:&quot;</span>, &amp;db);</span><br></pre></td></tr></table></figure>

<p><code>sqlite3_open</code> 函数的作用是：打开一个数据库。它的参数是数据库名称和数据库句柄。<br><strong>:memory:</strong> 是一个特殊的数据库名称，使用它可以新开一个数据库且该数据存放在内存中。函数的返回代码指示数据库是否已成功打开。成功打开并建立连接时返回 SQLITE_OK。</p>
<p>如果返回代码指示错误，这里我们会将消息打印到控制台，关闭数据库句柄，然后终止程序。<br><code>sqlite3_errmsg</code> 函数的作用是：返回错误的描述。<br>无论打开数据库连接句柄时是否发生错误，都应该通过将其传递给 <code>sqlite3_close()</code> 函数来释放与数据库连接句柄关联的资源。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">sqlite3_prepare_v2(db, <span class="string">&quot;SELECT SQLITE_VERSION()&quot;</span>, <span class="number">-1</span>, &amp;pStmt, <span class="number">0</span>);</span><br></pre></td></tr></table></figure>

<p>就像 C/C++ 或者 Java 那样，需要先编译才能执行命令。所以在执行 SQL 语句之前，必须首先使用 <code>sqlite3_prepare</code> 系列函数之一将其编译成字节码 (sqlite3_prepare() 函数已弃用)。</p>
<p><code>sqlite3_prepare_v2()</code> 函数接受五个参数：</p>
<ul>
<li>第一个参数是从 <code>sqlite3_open()</code> 函数获得的数据库句柄</li>
<li>第二个参数是要编译的 SQL 语句</li>
<li>第三个参数是 SQL 语句的最大长度 (以字节为单位)，传递 -1 会导致 SQL 连续地向后读取语句字符串，直到出现第一个 <code>&#39;\0&#39;</code> 终止符为止，也就是 C 字符串的末尾。根据文档说明，通过传递所提供字符串的确切字节数，可以获得一些较小的性能优势。</li>
<li>第四个参数是语句句柄。如果 <code>sqlite3_prepare_v2()</code> 运行成功，它将指向编译好的字节码语句。</li>
<li>最后一个参数是指向 SQL 语句未使用部分的指针。因为函数每次只编译 SQL 字符串的第一条语句，因此该参数指向剩下的未编译内容。我们传递 0，因为该参数对我们来说并不重要。</li>
</ul>
<p>如果成功，<code>sqlite3_prepare_v2()</code> 将返回 SQLITE_OK；否则将返回错误代码。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">sqlite3_step(pStmt);</span><br></pre></td></tr></table></figure>

<p><code>sqlite3_step()</code> 函数用来运行 SQL 语句。SQLITE_ROW 返回代码表示已经准备好下一行 (row) 数据，这说明我们的代码已经被正确执行。因为我们的 SQL 语句就为了获取版本号，它只返回一条数据，因此，我们只调用此函数一次。如果一次执行中可能会返回多条数据，我们可以进行多次调用，每调用一次就获取一条数据，SQL 内部会自动向下迭代。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">sqlite3_column_text(pStmt, <span class="number">0</span>);</span><br></pre></td></tr></table></figure>
<p>正确执行后 pStmt 句柄包含当前所迭代到的数据的索引信息，我们可以用它来访问该条数据信息。</p>
<p><code>sqlite3_finalize(res)</code> 函数的作用是：销毁我们准备好的语句对象。<br>最后<code>sqlite3_close()</code> 函数关闭了与数据库的连接。</p>
<h2 id="插入数据"><a href="#插入数据" class="headerlink" title="插入数据"></a>插入数据</h2><p>我们创建一个 CARS 表，并在其中插入几行。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// -- SQLite3 数据库插入数据的例子 --</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sqlite3.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    </span><br><span class="line">    sqlite3 *db;</span><br><span class="line">    <span class="keyword">char</span> *err_msg = <span class="number">0</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 打开数据库</span></span><br><span class="line">    <span class="keyword">if</span> (sqlite3_open(<span class="string">&quot;test.db&quot;</span>, &amp;db) != SQLITE_OK) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;Cannot open database: %s\n&quot;</span>, sqlite3_errmsg(db));</span><br><span class="line">        sqlite3_close(db);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 设置 SQL 语句字符串</span></span><br><span class="line">    <span class="keyword">char</span> *sql = <span class="string">&quot;DROP TABLE IF EXISTS Cars;&quot;</span> </span><br><span class="line">                <span class="string">&quot;CREATE TABLE Cars(Id INT, Name TEXT, Price INT);&quot;</span> </span><br><span class="line">                <span class="string">&quot;INSERT INTO Cars VALUES(1, &#x27;Audi&#x27;, 52642);&quot;</span> </span><br><span class="line">                <span class="string">&quot;INSERT INTO Cars VALUES(2, &#x27;Mercedes&#x27;, 57127);&quot;</span> </span><br><span class="line">                <span class="string">&quot;INSERT INTO Cars VALUES(3, &#x27;Skoda&#x27;, 9000);&quot;</span> </span><br><span class="line">                <span class="string">&quot;INSERT INTO Cars VALUES(4, &#x27;Volvo&#x27;, 29000);&quot;</span> </span><br><span class="line">                <span class="string">&quot;INSERT INTO Cars VALUES(5, &#x27;Bentley&#x27;, 350000);&quot;</span> </span><br><span class="line">                <span class="string">&quot;INSERT INTO Cars VALUES(6, &#x27;Citroen&#x27;, 21000);&quot;</span> </span><br><span class="line">                <span class="string">&quot;INSERT INTO Cars VALUES(7, &#x27;Hummer&#x27;, 41400);&quot;</span> </span><br><span class="line">                <span class="string">&quot;INSERT INTO Cars VALUES(8, &#x27;Volkswagen&#x27;, 21600);&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 编译并执行 SQL 语句字符串</span></span><br><span class="line">    <span class="keyword">if</span> (sqlite3_exec(db, sql, <span class="number">0</span>, <span class="number">0</span>, &amp;err_msg) != SQLITE_OK ) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;SQL error: %s\n&quot;</span>, err_msg);</span><br><span class="line">        sqlite3_free(err_msg);        </span><br><span class="line">        sqlite3_close(db);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    sqlite3_close(db);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>上面代码中，我们连接到 test.db 数据库，创建一个 CARS 表，并在创建的表中插入 8 行数据。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span> *err_msg = <span class="number">0</span>;</span><br></pre></td></tr></table></figure>
<p>如果出现错误，此指针将指向的错误消息。</p>
<p>然后是设置 SQL 语句字符串， 这些 SQL 语句会创建一个 CARS 表，并用数据填充它。语句必须用分号 <code>&#39;;&#39;</code>分隔。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">rc = sqlite3_exec(db, sql, <span class="number">0</span>, <span class="number">0</span>, &amp;err_msg);</span><br></pre></td></tr></table></figure>

<p><code>sqlite3_exec</code> 函数是 <code>sqlite3_prepare_v2</code>、<code>sqlite3_step</code> 和 <code>sqlite3_finalize</code> 的便捷封装方式，它允许应用程序运行 SQL 的多条语句，而不必使用大量 C 代码。</p>
<ul>
<li>第三个参数是一个回调函数，用于访问 SQL 语句中的每个结果行。</li>
<li>第四个参数是给回调函数的第一个参数。如果我们不需要它们，可以设置为 0 或者 NULL。</li>
<li>如果发生错误，则最后一个参数指向分配的错误消息。</li>
</ul>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">sqlite3_free(err_msg);</span><br></pre></td></tr></table></figure>
<p>必须使用 sqlite3_free() 函数调用释放分配的消息字符串。</p>
<p>我们可以使用 sqlite3 工具验证写入的数据。首先，我们修改数据在控制台中的显示方式。这里我们使用清爽的列对齐显示模式，并要求显示每条数据的标题。然后执行 <code>SELECT * FROM Cars;</code>语句。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">sqlite&gt; .mode column  </span><br><span class="line">sqlite&gt; .headers on</span><br><span class="line">sqlite&gt; SELECT * FROM Cars;</span><br><span class="line">Id          Name        Price     </span><br><span class="line">----------  ----------  ----------</span><br><span class="line"><span class="number">1</span>           Audi        <span class="number">52642</span>     </span><br><span class="line"><span class="number">2</span>           Mercedes    <span class="number">57127</span>     </span><br><span class="line"><span class="number">3</span>           Skoda       <span class="number">9000</span>      </span><br><span class="line"><span class="number">4</span>           Volvo       <span class="number">29000</span>     </span><br><span class="line"><span class="number">5</span>           Bentley     <span class="number">350000</span>    </span><br><span class="line"><span class="number">6</span>           Citroen     <span class="number">21000</span>     </span><br><span class="line"><span class="number">7</span>           Hummer      <span class="number">41400</span>     </span><br><span class="line"><span class="number">8</span>           Volkswagen  <span class="number">21600</span></span><br></pre></td></tr></table></figure>

<p>这就是我们写入CARS表的数据。</p>
<h2 id="最后插入的行ID"><a href="#最后插入的行ID" class="headerlink" title="最后插入的行ID"></a>最后插入的行ID</h2><p>有时，我们需要确定最后插入的行的 ID。为此，我们使用 <code>sqlite3_last_insert_rowid</code> 函数。如下</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> last_id = sqlite3_last_insert_rowid(db);</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;The last Id of the inserted row is %d\n&quot;</span>, last_id);</span><br></pre></td></tr></table></figure>

<p>下面语句执行后会在内存中创建一个 Friends 表。其 ID 列自动递增。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span> *sql = <span class="string">&quot;CREATE TABLE Friends(Id INTEGER PRIMARY KEY, Name TEXT);&quot;</span></span><br><span class="line">            <span class="string">&quot;INSERT INTO Friends(Name) VALUES (&#x27;Tom&#x27;);&quot;</span></span><br><span class="line">            <span class="string">&quot;INSERT INTO Friends(Name) VALUES (&#x27;Rebecca&#x27;);&quot;</span></span><br><span class="line">            <span class="string">&quot;INSERT INTO Friends(Name) VALUES (&#x27;Jim&#x27;);&quot;</span></span><br><span class="line">            <span class="string">&quot;INSERT INTO Friends(Name) VALUES (&#x27;Roger&#x27;);&quot;</span></span><br><span class="line">            <span class="string">&quot;INSERT INTO Friends(Name) VALUES (&#x27;Robert&#x27;);&quot;</span>;</span><br></pre></td></tr></table></figure>

<p>在 SQLite 中，整数主键 (PRIMARY KEY) 列会自动递增。还有一个 <code>AUTOINCREMENT</code> 关键字。当该关键字应用于整数主键时，使用的 ID 创建算法略有不同。当使用自动递增列时，我们需要显式地声明列名。</p>
<h2 id="检索数据"><a href="#检索数据" class="headerlink" title="检索数据"></a>检索数据</h2><p>我们已经将一些数据插入到 test.db 数据库中。在下面的示例中，我们将从数据库检索数据。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// -- 检索数据 Callback方法</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sqlite3.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">callback</span><span class="params">(<span class="keyword">void</span> *, <span class="keyword">int</span>, <span class="keyword">char</span> **, <span class="keyword">char</span> **)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    sqlite3 *db;</span><br><span class="line">    <span class="keyword">char</span> *err_msg = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 打开数据库</span></span><br><span class="line">    <span class="keyword">if</span> (sqlite3_open(<span class="string">&quot;test.db&quot;</span>, &amp;db) != SQLITE_OK) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;Cannot open database: %s\n&quot;</span>, </span><br><span class="line">                sqlite3_errmsg(db));</span><br><span class="line">        sqlite3_close(db);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 设置语句并编译执行</span></span><br><span class="line">    <span class="keyword">char</span> *sql = <span class="string">&quot;SELECT * FROM Cars;&quot;</span>;</span><br><span class="line">    <span class="keyword">if</span> (sqlite3_exec(db, sql, callback, <span class="number">0</span>, &amp;err_msg) != SQLITE_OK ) &#123;    </span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;Failed to select data\n&quot;</span>);</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;SQL error: %s\n&quot;</span>, err_msg);</span><br><span class="line">        sqlite3_free(err_msg);</span><br><span class="line">        sqlite3_close(db);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125; </span><br><span class="line"></span><br><span class="line">    sqlite3_close(db);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 回调函数</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">callback</span><span class="params">(<span class="keyword">void</span> *NotUsed, <span class="keyword">int</span> argc, <span class="keyword">char</span> **argv, <span class="keyword">char</span> **azColName)</span> </span>&#123;</span><br><span class="line">    NotUsed = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; argc; i++)</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%s = %s\n&quot;</span>, azColName[i], argv[i] ? argv[i] : <span class="string">&quot;NULL&quot;</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;\n&quot;</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我们使用 <code>SELECT * FROM Cars;</code> 语句从 CARS 表中检索所有行。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">callback</span><span class="params">(<span class="keyword">void</span> *, <span class="keyword">int</span>, <span class="keyword">char</span> **, <span class="keyword">char</span> **)</span></span>;</span><br></pre></td></tr></table></figure>
<p>这是与 sqlite3_exec() 函数结合使用的回调函数的函数原型。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">sqlite3_exec(db, sql, callback, <span class="number">0</span>, &amp;err_msg);</span><br></pre></td></tr></table></figure>
<p>这次 <code>sqlite3_exec</code> 函数中的我们加入了回调函数，sqlite3_exec 在处理求值语句时每得到一个结果行将调用一次 <code>callback</code> 函数。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">callback</span><span class="params">(<span class="keyword">void</span> *NotUsed, <span class="keyword">int</span> argc, <span class="keyword">char</span> **argv, <span class="keyword">char</span> **azColName)</span> </span>&#123;</span><br><span class="line">    NotUsed = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; argc; i++)</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%s = %s\n&quot;</span>, azColName[i], argv[i] ? argv[i] : <span class="string">&quot;NULL&quot;</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;\n&quot;</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>来看这个回调函数</p>
<ul>
<li>第一个参数是 <code>sqlite3_exec</code> 的第四个参数中提供的数据，通常不使用它。</li>
<li>第二个参数是结果行中列的个数。</li>
<li>第三个参数是表示结果行中字段的字符串数组。它包含着我们每一行的数据。</li>
<li>最后一个参数就是我们设置的标题行，该行的每一列保存着每一列数据的标题名称。</li>
</ul>
<p>所以上面代码中，我们遍历<strong>结果行</strong>和<strong>标题行</strong>中的所有列就并打印出了它们的名称和内容。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">$ ./select_all </span><br><span class="line">Id = <span class="number">1</span></span><br><span class="line">Name = Audi</span><br><span class="line">Price = <span class="number">52642</span></span><br><span class="line"></span><br><span class="line">Id = <span class="number">2</span></span><br><span class="line">Name = Mercedes</span><br><span class="line">Price = <span class="number">57127</span></span><br><span class="line"></span><br><span class="line">...</span><br></pre></td></tr></table></figure>
<p>以上是示例的部分输出。</p>
<h2 id="参数化查询"><a href="#参数化查询" class="headerlink" title="参数化查询"></a>参数化查询</h2><p>现在我们将用到参数化查询。参数化查询，也称为预准备语句 (prepared statements), 可提高安全性和性能。当我们使用参数化查询时，我们使用占位符，而不是直接将值写入语句。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// -- SQLite3 参数化查询例子 --</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sqlite3.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    sqlite3 *db;</span><br><span class="line">    <span class="keyword">char</span> *err_msg = <span class="number">0</span>;</span><br><span class="line">    sqlite3_stmt *pStmt;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 打开数据库</span></span><br><span class="line">    <span class="keyword">if</span> (sqlite3_open(<span class="string">&quot;test.db&quot;</span>, &amp;db) != SQLITE_OK) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;Cannot open database: %s\n&quot;</span>, sqlite3_errmsg(db));</span><br><span class="line">        sqlite3_close(db);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 设置并预编译 SQL 语句</span></span><br><span class="line">    <span class="keyword">char</span> *sql = <span class="string">&quot;SELECT Id, Name FROM Cars WHERE Id = ?&quot;</span>;</span><br><span class="line">    <span class="keyword">if</span> (sqlite3_prepare_v2(db, sql, <span class="number">-1</span>, &amp;pStmt, <span class="number">0</span>) == SQLITE_OK)</span><br><span class="line">        <span class="comment">// 如果预编译成功，则对占位符进行绑定：第一个占位符，设置为 3</span></span><br><span class="line">        sqlite3_bind_int(pStmt, <span class="number">1</span>, <span class="number">3</span>);</span><br><span class="line">    <span class="keyword">else</span> </span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;Failed to execute statement: %s\n&quot;</span>, sqlite3_errmsg(db));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 执行语句获取结果行，如果数据可用则获取数据</span></span><br><span class="line">    <span class="keyword">if</span> (sqlite3_step(pStmt) == SQLITE_ROW) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%s: &quot;</span>, sqlite3_column_text(pStmt, <span class="number">0</span>));</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%s\n&quot;</span>, sqlite3_column_text(pStmt, <span class="number">1</span>));</span><br><span class="line">    &#125; </span><br><span class="line"></span><br><span class="line">    sqlite3_finalize(pStmt);</span><br><span class="line">    sqlite3_close(db);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在本例中，问号 <code>&#39;?&#39;</code> 用作占位符，稍后将其替换为实际值并为 SQL 查询提供 Id。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span> *sql = <span class="string">&quot;SELECT Id, Name FROM Cars WHERE Id = ?&quot;</span>;</span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">sqlite3_bind_int(pStmt, <span class="number">1</span>, <span class="number">3</span>);</span><br></pre></td></tr></table></figure>

<p><code>sqlite3_bind_int</code> 将整数值绑定到准备好的语句，占位符替换为整数值3。</p>
<ul>
<li>第二个参数表明要设置第几个占位符，这里我们只有一个占位符</li>
<li>第三个参数表明要把占位符替换成什么</li>
</ul>
<p><code>sqlite3_step</code> 函数将执行处理好的 SQL 语句，并索引到执行结果行。</p>
<p>如果有某行数据可用，我们将使用 <code>sqlite3_column_text</code> 函数获取结果行中的两列的值，即</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%s: &quot;</span>, sqlite3_column_text(res, <span class="number">0</span>));</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%s\n&quot;</span>, sqlite3_column_text(res, <span class="number">1</span>));</span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">$ ./parameterized </span><br><span class="line"><span class="number">3</span>: Skoda</span><br></pre></td></tr></table></figure>
<p>正如所期待的，该示例返回第三个数据的 Id 和汽车的名称，因为我们绑定到占位符的数据是 3 。</p>
<p>第二个示例使用<strong>带有命名占位符</strong>的参数化语句</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// -- SQLite3 使用命名占位例子 --</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sqlite3.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span> </span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    </span><br><span class="line">    sqlite3 *db;</span><br><span class="line">    <span class="keyword">char</span> *err_msg = <span class="number">0</span>;</span><br><span class="line">    sqlite3_stmt *pStmt;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 打开数据库</span></span><br><span class="line">    <span class="keyword">if</span> (sqlite3_open(<span class="string">&quot;test.db&quot;</span>, &amp;db) != SQLITE_OK) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;Cannot open database: %s\n&quot;</span>, sqlite3_errmsg(db));</span><br><span class="line">        sqlite3_close(db);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 设置并预编译 SQL 语句</span></span><br><span class="line">    <span class="keyword">char</span> *sql = <span class="string">&quot;SELECT Id, Name FROM Cars WHERE Id = @id&quot;</span>;</span><br><span class="line">    <span class="keyword">if</span> (sqlite3_prepare_v2(db, sql, <span class="number">-1</span>, &amp;pStmt, <span class="number">0</span>) != SQLITE_OK) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;Failed to execute statement: %s\n&quot;</span>, sqlite3_errmsg(db));</span><br><span class="line">        sqlite3_close(db);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 通过占位符名 @name 获取占位符位置</span></span><br><span class="line">    <span class="keyword">int</span> idx = sqlite3_bind_parameter_index(pStmt, <span class="string">&quot;@id&quot;</span>);</span><br><span class="line">    <span class="keyword">int</span> value = <span class="number">4</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 把整数值绑定到语句中的占位符位置</span></span><br><span class="line">    sqlite3_bind_int(pStmt, idx, value);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 执行语句获取结果行，如果数据可用则打印数据</span></span><br><span class="line">    <span class="keyword">if</span> (sqlite3_step(pStmt) == SQLITE_ROW) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%s: &quot;</span>, sqlite3_column_text(pStmt, <span class="number">0</span>));</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%s\n&quot;</span>, sqlite3_column_text(pStmt, <span class="number">1</span>));</span><br><span class="line">    &#125; </span><br><span class="line"></span><br><span class="line">    sqlite3_finalize(pStmt);</span><br><span class="line">    sqlite3_close(db);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>命名占位符的前缀可以是 <code>&#39;@&#39;</code>, 也可以是冒号 <code>&#39;:&#39;</code>。 </p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> idx = sqlite3_bind_parameter_index(pStmt, <span class="string">&quot;@id&quot;</span>);</span><br></pre></td></tr></table></figure>
<p><code>sqlite3_bind_parameter_index</code> 函数的作用是返回给定占位符名称的 SQL 占位符的索引。</p>
<h2 id="插入图片"><a href="#插入图片" class="headerlink" title="插入图片"></a>插入图片</h2><p>在本节中，我们将向 SQLite 数据库插入图像。请注意，有些人反对将图像放入数据库。这里我们只演示如何做到这一点。我们没有详细讨论是否应该将图像保存在数据库中的技术问题。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">sqlite&gt; <span class="function">CREATE TABLE <span class="title">Images</span><span class="params">(Id INTEGER PRIMARY KEY, Data BLOB)</span></span>;</span><br></pre></td></tr></table></figure>

<p>对于本例，我们创建一个名为 Images 的新表。其中我们使用 BLOB 数据类型来存放图片，它代表二进制大对象。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sqlite3.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    FILE *fp = fopen(<span class="string">&quot;woman.jpg&quot;</span>, <span class="string">&quot;rb&quot;</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (fp == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;Cannot open image file\n&quot;</span>);    </span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 获取文件大小，在这里为了简洁，我们没有对获取过程</span></span><br><span class="line">    <span class="comment">// 可能出现的出错进行处理，实际开发应注意这一点</span></span><br><span class="line">    fseek(fp, <span class="number">0</span>, SEEK_END);</span><br><span class="line">    <span class="keyword">int</span> fsize = ftell(fp);</span><br><span class="line">    fseek(fp, <span class="number">0</span>, SEEK_SET);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 根据获取文件大小获取内存，并将文件读入到内存</span></span><br><span class="line">    <span class="keyword">char</span> *data = (<span class="keyword">char</span>*)<span class="built_in">malloc</span>(<span class="keyword">sizeof</span>(<span class="keyword">char</span>)*fsize);</span><br><span class="line">    <span class="keyword">if</span>(data == <span class="literal">NULL</span> || fread(data, <span class="number">1</span>, fsize, fp) != fsize) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;malloc or readfile error\n&quot;</span>);</span><br><span class="line">        fclose(fp);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    fclose(fp);</span><br><span class="line"></span><br><span class="line">    sqlite3 *db;</span><br><span class="line">    <span class="keyword">char</span> *err_msg = <span class="number">0</span>;</span><br><span class="line">    sqlite3_stmt *pStmt;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 打开数据库</span></span><br><span class="line">    <span class="keyword">if</span> (sqlite3_open(<span class="string">&quot;test.db&quot;</span>, &amp;db) != SQLITE_OK) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;Cannot open database: %s\n&quot;</span>, sqlite3_errmsg(db));</span><br><span class="line">        sqlite3_close(db);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 语句预处理</span></span><br><span class="line">    <span class="keyword">char</span> *sql = <span class="string">&quot;INSERT INTO Images(Data) VALUES(?)&quot;</span>;    </span><br><span class="line">    <span class="keyword">if</span> (sqlite3_prepare(db, sql, <span class="number">-1</span>, &amp;pStmt, <span class="number">0</span>) != SQLITE_OK) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;Cannot prepare statement: %s\n&quot;</span>, sqlite3_errmsg(db));</span><br><span class="line">        sqlite3_close(db);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;    </span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 绑定占位符并插入内存中的二进制文件</span></span><br><span class="line">    sqlite3_bind_blob(pStmt, <span class="number">1</span>, data, fsize, SQLITE_STATIC);</span><br><span class="line">    <span class="keyword">if</span> (sqlite3_step(pStmt) != SQLITE_DONE)</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;execution failed: %s&quot;</span>, sqlite3_errmsg(db));</span><br><span class="line">    </span><br><span class="line">    sqlite3_finalize(pStmt);    </span><br><span class="line">    sqlite3_close(db);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在此程序中，我们从当前工作目录读取图像到堆内存中，之后将其写入 SQLite test.db 数据库的 Images 表。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">sqlite3_bind_blob(pStmt, <span class="number">1</span>, data, size, SQLITE_STATIC);</span><br></pre></td></tr></table></figure>
<p><code>sqlite3_bind_blob</code> 函数的作用是：将二进制数据绑定到编译后的语句中的占位符。SQLITE_STATIC 参数表示指向内容信息的指针是静态的，不需要由 SQLite 释放，这将由我们主动释放。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">sqlite3_step(pStmt);</span><br></pre></td></tr></table></figure>
<p>最后调用 <code>sqlite3_step</code> 执行上面处理好的 SQLite 语句 <code>pStmt</code> 便可以将图形写入到表中。</p>
<h2 id="读取图像"><a href="#读取图像" class="headerlink" title="读取图像"></a>读取图像</h2><p>在本节中，我们将执行相反的操作，从数据库表中读取图像。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sqlite3.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    FILE *fp = fopen(<span class="string">&quot;2233.jpg&quot;</span>, <span class="string">&quot;wb&quot;</span>);</span><br><span class="line">    <span class="keyword">if</span> (fp == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;Cannot open image file\n&quot;</span>);    </span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;    </span><br><span class="line">    </span><br><span class="line">    sqlite3 *db;</span><br><span class="line">    <span class="keyword">char</span> *err_msg = <span class="number">0</span>;</span><br><span class="line">    sqlite3_stmt *pStmt;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 打开数据库</span></span><br><span class="line">    <span class="keyword">if</span> (sqlite3_open(<span class="string">&quot;test.db&quot;</span>, &amp;db) != SQLITE_OK) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;Cannot open database: %s\n&quot;</span>, sqlite3_errmsg(db));</span><br><span class="line">        sqlite3_close(db);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 语句预处理</span></span><br><span class="line">    <span class="keyword">char</span> *sql = <span class="string">&quot;SELECT Data FROM Images WHERE Id = 1&quot;</span>;</span><br><span class="line">    <span class="keyword">if</span> (sqlite3_prepare_v2(db, sql, <span class="number">-1</span>, &amp;pStmt, <span class="number">0</span>) != SQLITE_OK) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;Cannot prepare statement: %s\n&quot;</span>, sqlite3_errmsg(db));</span><br><span class="line">        sqlite3_close(db);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 执行语句</span></span><br><span class="line">    <span class="keyword">if</span> (sqlite3_step(pStmt) == SQLITE_ROW) &#123;</span><br><span class="line">        <span class="comment">// 先获取对应列数据的大小</span></span><br><span class="line">        <span class="keyword">int</span> bytes = sqlite3_column_bytes(pStmt, <span class="number">0</span>);</span><br><span class="line">        <span class="comment">// 获取二进制文件并将其写入文件</span></span><br><span class="line">        fwrite(sqlite3_column_blob(pStmt, <span class="number">0</span>), bytes, <span class="number">1</span>, fp);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    fclose(fp);</span><br><span class="line">    sqlite3_finalize(pStmt);   </span><br><span class="line">    sqlite3_close(db);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">bytes = sqlite3_column_bytes(pStmt, <span class="number">0</span>);</span><br><span class="line">fwrite(sqlite3_column_blob(pStmt, <span class="number">0</span>), bytes, <span class="number">1</span>, fp);</span><br></pre></td></tr></table></figure>
<p><code>sqlite3_column_bytes</code> 函数的作用是：返回 BLOB 中的字节数。<br><code>sqlite3_column_blob</code> 函数返回指向所选二进制数据的指针。</p>
<h2 id="元数据"><a href="#元数据" class="headerlink" title="元数据"></a>元数据</h2><p>元数据是关于数据库中数据的信息。SQLite 中的元数据包含有关表和列的信息，我们在这些表和列中存储数据。受 SQL 语句影响的行数是元数据。结果集中返回的行数和列数也属于元数据。</p>
<p>可以使用 Pragma 命令获取 SQLite 中的元数据。SQLite 对象可能具有属性，这些属性是元数据。最后，我们还可以通过查询 SQLite 系统 sqlite_master 表来获得特定的元数据。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sqlite3.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">callback</span><span class="params">(<span class="keyword">void</span> *, <span class="keyword">int</span>, <span class="keyword">char</span> **, <span class="keyword">char</span> **)</span></span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    sqlite3 *db;</span><br><span class="line">    <span class="keyword">char</span> *err_msg = <span class="number">0</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 打开数据库</span></span><br><span class="line">    <span class="keyword">if</span> (sqlite3_open(<span class="string">&quot;test.db&quot;</span>, &amp;db) != SQLITE_OK) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;Cannot open database: %s\n&quot;</span>, sqlite3_errmsg(db));</span><br><span class="line">        sqlite3_close(db);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 编译并执行语句</span></span><br><span class="line">    <span class="keyword">char</span> *sql = <span class="string">&quot;PRAGMA table_info(Cars);&quot;</span>;</span><br><span class="line">    <span class="keyword">if</span> (sqlite3_exec(db, sql, callback, <span class="number">0</span>, &amp;err_msg) != SQLITE_OK ) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;Failed to select data %s\n&quot;</span>, err_msg);</span><br><span class="line">        sqlite3_free(err_msg);</span><br><span class="line">        sqlite3_close(db);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    sqlite3_close(db);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">callback</span><span class="params">(<span class="keyword">void</span> *NotUsed, <span class="keyword">int</span> argc, <span class="keyword">char</span> **argv, <span class="keyword">char</span> **azColName)</span> </span></span><br><span class="line"><span class="function"></span>&#123;    </span><br><span class="line">    NotUsed = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; argc; i++)</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%s = %s\n&quot;</span>, azColName[i], argv[i] ? argv[i] : <span class="string">&quot;NULL&quot;</span>);</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;------------\n&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>在本例中，我们执行语句 <code>PRAGMA table_info(Cars);</code>，以获取有关 CARS 表的一些元数据信息。结果集中的列包括列序号、列名、数据类型、列是否可以为NULL以及列的默认值。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">$ ./column_names</span><br><span class="line">cid = <span class="number">0</span></span><br><span class="line">name = Id</span><br><span class="line">type = INT</span><br><span class="line">notnull = <span class="number">0</span></span><br><span class="line">dflt_value = <span class="literal">NULL</span></span><br><span class="line">pk = <span class="number">1</span></span><br><span class="line">------------</span><br><span class="line">cid = <span class="number">1</span></span><br><span class="line">name = Name</span><br><span class="line">type = TEXT</span><br><span class="line">notnull = <span class="number">0</span></span><br><span class="line">dflt_value = <span class="literal">NULL</span></span><br><span class="line">pk = <span class="number">0</span></span><br><span class="line">------------</span><br><span class="line">cid = <span class="number">2</span></span><br><span class="line">name = Price</span><br><span class="line">type = INT</span><br><span class="line">notnull = <span class="number">0</span></span><br><span class="line">dflt_value = <span class="literal">NULL</span></span><br><span class="line">pk = <span class="number">0</span></span><br><span class="line">------------</span><br></pre></td></tr></table></figure>

<p>在下一个与元数据相关的示例中，我们将列出test.db数据库中的所有表</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sqlite3.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">callback</span><span class="params">(<span class="keyword">void</span> *, <span class="keyword">int</span>, <span class="keyword">char</span> **, <span class="keyword">char</span> **)</span></span>;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    sqlite3 *db;</span><br><span class="line">    <span class="keyword">char</span> *err_msg = <span class="number">0</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (sqlite3_open(<span class="string">&quot;test.db&quot;</span>, &amp;db) != SQLITE_OK) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;Cannot open database: %s\n&quot;</span>, sqlite3_errmsg(db));</span><br><span class="line">        sqlite3_close(db);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">char</span> *sql = <span class="string">&quot;SELECT name FROM sqlite_master WHERE type=&#x27;table&#x27;&quot;</span>;</span><br><span class="line">    <span class="keyword">if</span> (sqlite3_exec(db, sql, callback, <span class="number">0</span>, &amp;err_msg) != SQLITE_OK ) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;SQL error: %s\n&quot;</span>, err_msg);</span><br><span class="line">        sqlite3_free(err_msg);</span><br><span class="line">        sqlite3_close(db);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125; </span><br><span class="line">    </span><br><span class="line">    sqlite3_close(db);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">callback</span><span class="params">(<span class="keyword">void</span> *NotUsed, <span class="keyword">int</span> argc, <span class="keyword">char</span> **argv, <span class="keyword">char</span> **azColName)</span> </span>&#123;</span><br><span class="line">    NotUsed = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; argc; i++) </span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%s\n&quot;</span>, argv[i] ? argv[i] : <span class="string">&quot;NULL&quot;</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>代码示例将当前数据库中的所有可用表打印到终端。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">$ ./list_tables </span><br><span class="line">Friends</span><br><span class="line">Cars</span><br><span class="line">Images</span><br></pre></td></tr></table></figure>
<p>以上就是之前操作的数据内容。</p>
<h2 id="事务"><a href="#事务" class="headerlink" title="事务"></a>事务</h2><p>事务是针对一个或多个数据库中的数据执行的数据库操作的原子单元。事务中所有 SQL 语句的效果可以全部提交到数据库，也可以全部回滚。</p>
<p>在 SQLite 中，除 SELECT 之外的任何命令都将启动隐式事务。此外，在事务中，像 create table.、Vacuum、Pragma 这样的命令将在执行之前提交以前的更改。</p>
<p>手动事务以 BEGIN TRANSACTION 语句开始，以 COMMIT 或 ROLLBACK 语句结束。</p>
<p>SQLite 支持三个非标准事务级别：延迟、立即和独占。</p>
<h2 id="自动提交"><a href="#自动提交" class="headerlink" title="自动提交"></a>自动提交</h2><p>默认情况下，SQLite 版本 3 在自动提交模式下运行。在自动提交模式下，与当前数据库连接关联的所有操作完成后，立即提交对数据库的所有更改。自动提交模式由 BEGIN 语句禁用，由 COMMIT 或 ROLLBACK 重新启用。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sqlite3.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    sqlite3 *db;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (sqlite3_open(<span class="string">&quot;test.db&quot;</span>, &amp;db) != SQLITE_OK) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;Cannot open database: %s\n&quot;</span>, sqlite3_errmsg(db));</span><br><span class="line">        sqlite3_close(db);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;    </span><br><span class="line">    </span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;Autocommit: %d\n&quot;</span>, sqlite3_get_autocommit(db));</span><br><span class="line">    sqlite3_close(db);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;Autocommit: %d\n&quot;</span>, sqlite3_get_autocommit(db));</span><br></pre></td></tr></table></figure>
<p>此示例检查数据库是否处于自动提交模式，如果数据库未处于自动提交模式，则 sqlite3_get_autoCommit() 函数返回零。如果处于自动提交模式，则返回非零值。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">$ ./get_ac_mode </span><br><span class="line">Autocommit: <span class="number">1</span></span><br></pre></td></tr></table></figure>
<p>该示例确认 SQLite 默认处于自动提交模式。</p>
<p>下一个示例进一步阐明了自动提交模式。在自动提交模式下，每个非 SELECT 语句都是一个立即提交的小事务。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sqlite3.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    sqlite3 *db;</span><br><span class="line">    <span class="keyword">char</span> *err_msg = <span class="number">0</span>;</span><br><span class="line">    </span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (sqlite3_open(<span class="string">&quot;test.db&quot;</span>, &amp;db) != SQLITE_OK) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;Cannot open database: %s\n&quot;</span>, sqlite3_errmsg(db));</span><br><span class="line">        sqlite3_close(db);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">char</span> *sql = <span class="string">&quot;DROP TABLE IF EXISTS Friends;&quot;</span> </span><br><span class="line">                <span class="string">&quot;CREATE TABLE Friends(Id INTEGER PRIMARY KEY, Name TEXT);&quot;</span> </span><br><span class="line">                <span class="string">&quot;INSERT INTO Friends(Name) VALUES (&#x27;Tom&#x27;);&quot;</span> </span><br><span class="line">                <span class="string">&quot;INSERT INTO Friends(Name) VALUES (&#x27;Rebecca&#x27;);&quot;</span> </span><br><span class="line">                <span class="string">&quot;INSERT INTO Friends(Name) VALUES (&#x27;Jim&#x27;);&quot;</span> </span><br><span class="line">                <span class="string">&quot;INSERT INTO Friend(Name) VALUES (&#x27;Robert&#x27;);&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (sqlite3_exec(db, sql, <span class="number">0</span>, <span class="number">0</span>, &amp;err_msg) != SQLITE_OK ) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;SQL error: %s\n&quot;</span>, err_msg);</span><br><span class="line">        sqlite3_free(err_msg);        </span><br><span class="line">        sqlite3_close(db);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125; </span><br><span class="line">    </span><br><span class="line">    sqlite3_close(db);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>我们创建 Friends 表，并尝试用数据填充它，最后一条 SQL 语句有一个错误；因为没有 Friend 表。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">$ ./autocommit </span><br><span class="line">SQL error: no such table: Friend</span><br><span class="line">$ sqlite3 test.db</span><br><span class="line">sqlite&gt; .tables</span><br><span class="line">Cars     Friends  Images </span><br><span class="line">sqlite&gt; SELECT * FROM Friends;</span><br><span class="line"><span class="number">1</span>|Tom</span><br><span class="line"><span class="number">2</span>|Rebecca</span><br><span class="line"><span class="number">3</span>|Jim</span><br></pre></td></tr></table></figure>
<p>因为每插入一个就自动提交一次，所以虽然插入最后一个数据的时候发生了错误，但是前面的三个数据已经提交到数据库里了。</p>
<h2 id="交易"><a href="#交易" class="headerlink" title="交易"></a>交易</h2><p>在下一个示例中，我们将一些 SQL 语句插入到事务中</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sqlite3.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">void</span>)</span> </span>&#123;</span><br><span class="line">    </span><br><span class="line">    sqlite3 *db;</span><br><span class="line">    <span class="keyword">char</span> *err_msg = <span class="number">0</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">if</span> (sqlite3_open(<span class="string">&quot;test.db&quot;</span>, &amp;db) != SQLITE_OK) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;Cannot open database: %s\n&quot;</span>, sqlite3_errmsg(db));</span><br><span class="line">        sqlite3_close(db);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">char</span> *sql = <span class="string">&quot;DROP TABLE IF EXISTS Friends;&quot;</span></span><br><span class="line">                <span class="string">&quot;BEGIN TRANSACTION;&quot;</span> </span><br><span class="line">                <span class="string">&quot;CREATE TABLE Friends(Id INTEGER PRIMARY KEY, Name TEXT);&quot;</span> </span><br><span class="line">                <span class="string">&quot;INSERT INTO Friends(Name) VALUES (&#x27;Tom&#x27;);&quot;</span> </span><br><span class="line">                <span class="string">&quot;INSERT INTO Friends(Name) VALUES (&#x27;Rebecca&#x27;);&quot;</span> </span><br><span class="line">                <span class="string">&quot;INSERT INTO Friends(Name) VALUES (&#x27;Jim&#x27;);&quot;</span> </span><br><span class="line">                <span class="string">&quot;INSERT INTO Friend(Name) VALUES (&#x27;Robert&#x27;);&quot;</span></span><br><span class="line">                <span class="string">&quot;COMMIT;&quot;</span>;</span><br><span class="line">                           </span><br><span class="line">    <span class="keyword">if</span> (sqlite3_exec(db, sql, <span class="number">0</span>, <span class="number">0</span>, &amp;err_msg) != SQLITE_OK ) &#123;</span><br><span class="line">        <span class="built_in">fprintf</span>(<span class="built_in">stderr</span>, <span class="string">&quot;SQL error: %s\n&quot;</span>, err_msg);</span><br><span class="line">        sqlite3_free(err_msg);        </span><br><span class="line">        sqlite3_close(db);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">        </span><br><span class="line">    </span><br><span class="line">    sqlite3_close(db);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<p>第一个语句如果存在表 <code>Friends</code> 则将其删除。其他语句则放在事务中。事务在全有或全无模式下工作。要么什么都不做，要么什么都做。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line">sqlite&gt; .tables</span><br><span class="line">Cars    Images</span><br></pre></td></tr></table></figure>
<p>因为最后一条语句有错误，所以事务被回滚，并且没有创建Friends表。</p>
]]></content>
      <categories>
        <category>Lua</category>
      </categories>
      <tags>
        <tag>Sqlite3</tag>
      </tags>
  </entry>
</search>
